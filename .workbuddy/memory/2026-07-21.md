
# 2026-07-21 皮质醇 Need 日志排查

## 关键结论
- rimsage MCP 初始化成功（本会话查询返回 RimWorld 源码，可用）。
- **调试 DLL 的复用技巧（重要）**：编译后的 RimWorld 模组 DLL 里，
  类型/成员名在 #Strings 堆（UTF-8），但**用户字符串字面量（Log 文案等）在 #US 堆，编码为 UTF-16LE**。
  用  或 ASCII 字节搜会全盘 MISSING，导致误判“DLL 没编进我的代码”。
  正确做法：用 Python  校验字面量，ASCII 校验类型名。
- 用 rimsage 核实 ：Cortisol NeedDef（minIntelligence=ToolUser(1) ≤ Humanlike(2)、developmentalStageFilter 默认 Child|Adult）对该殖民者返回 true，Need 本应被加入。

## 本次改动（诊断用，确认后可删）
- ：构造/SetInitialLevel/首次 NeedInterval/IsFrozen/异常 均加了 [Cortisol-Init]/[Cortisol-Tick] 日志，NeedInterval 包了 try/catch。
- 新增 ：postfix 追踪 ，报告 Cortisol NeedDef 是否加载、各 pawn 是否拿到 Cortisol Need（已加入 csproj 编译列表）。
- 干净重建 + 部署到游戏 Mods/Assemblies（23:52）。

## 待用户运行时回报
- 看游戏日志里 [Cortisol-Init]/[Cortisol-Tick]/[Cortisol-飘字检查] 是否出现；
- 重点看 Error 日志有没有 "Error while determining if ... should have Need Cortisol"（Activator.CreateInstance 抛异常被吞）。
- 仍零日志 => Need 未被加入 pawn（最可能 AddNeed 实例化异常被吞，或 Def 未加载）。

## 23:48 运行日志（Player.log）关键发现
- 日志确认 `[Hormones] Assembly loaded: RimHormones, Version=1.0.0.0`（Line 51）→ mod 的 Assembly **确实加载**。
- 但**整份 23:48 日志零 Cortisol 字样** → 运行期皮质醇 Need 从未被实例化（AddNeed 未执行或异常被吞）。配置层全对，纯运行期问题。
- ModsConfig.xml：`thgold.hormones` 已激活；游戏从 `D:\Steam\steamapps\common\RimWorld\Mods\Rim-Hormones` 读 mod（与部署路径一致，排除“部署到错误副本”假设）。
- 日志另有无关噪音：`Backstory_Physique.xml` 一个 Patch 失败、`SevereBleeding ThoughtDef not found`（mod 自带 warning，与皮质醇无关）。

## 增强诊断（23:58 部署）
- 重写 `Harmony_CortisolNeedInit.cs`：新增 `[StaticConstructorOnStartup] CortisolNeed_ModLoadTrace`，启动即打印 `[Cortisol-ModLoad] DLL 已加载 | Cortisol NeedDef 已加载=… | 类型可访问=…`（确认 Def 是否进 DefDatabase）。
- postfix `AddOrRemoveNeedsAsAppropriate` 优化：去重报告每个 pawn 是否拿到 Cortisol Need，并在“未拥有”时诊断 ShouldHaveNeed 失败原因（intelligence/devStage/colonistsOnly/DisablesNeed）。
- 干净重建 0 错误；部署 DLL 含 Cortisol-ModLoad/Cortisol-Init/Cortisol-Tick/Cortisol-飘字检查。

## 下一步（待用户跑一次确认根因）
- 用户重启游戏→进主世界→跑一会，看日志 `[Cortisol-ModLoad]`（启动即有）与 `[Cortisol-Init]`（殖民者生成时）。
- 若 ModLoad 显示 Def 未加载 → 修 Need_Cortisol.xml；若 Init 显示“未拥有”且有 ShouldHaveNeed 失败原因 → 放宽 NeedDef；若两者都正常但仍无飘字 → 查 IsFrozen / NeedInterval 调用频率。
