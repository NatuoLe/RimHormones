# 2026-08-20 工作日志

## MEE 水系统 3 个修复（取水/水瓶）

### 问题1+2：设施灌装瞬时 + 取10出20（根因 `WaterAdapter.cs::BuildMEEWaterFillToils`）
- 原 toil 是 `ToilCompleteMode.Instant` + initAction 一次性扣全部水、硬编码 `maxBottles=20` 忽略 `job.count` → 取10出20、且一瞬间完成。
- 改为：走到设施后建一个 `Delay` toil，`defaultDuration = bottlesToMake*90tick`；每 90 tick 拉 0.5L 管网水、产 1 瓶 `MEE_WaterBottle`（优先并入已有同类堆叠，落点占则 8 邻格找空位）。仿 DBH「一瓶一瓶+带耗时」。
- 产瓶数 = `Math.Min(Math.Min(job.count>0?job.count:50, floor(netWater/0.5)), 50)`：尊重请求量、受管网实际水量与安全上限(50)约束。管网中途抽干则 `remaining=0` 提前停止、保留已产。
- 构建通过（模块 MetabolicEssential.dll）。

### 问题3：炉灶需水但无 Job 主动取水
- 仿 FDE `WorkGiver_FillWaterBottles` 新建主工程 `WorkGiver_MEE_FillWaterBottles.cs`（必须放主工程，否则 MEE 关时 Def 解析失败）：
  - `ShouldSkip` 门控 `MetaBolicLoadCtrl.Active` 且 `DBHStockpileWaterBottles` JobDef 存在（无 DBH 即下线），零硬引用 DBH。
  - 扫描 `allBuildingsColonist` 的 `IBillGiver.BillStack`，累计需 MEE_RawWater/MEE_WaterBottle 的活动账单需求(`RemainingIterations`×单次)，与地图地面水库存比，差量>0 才触发。
  - `HasJobOnThing` 找带 CompPipe 的 DBH 设施(用 AllComps 遍历，避 `Thing.GetComp(Type)` 本版本不可用)，校验 CanReserve/CanReach。
  - `JobOnThing` 派 `DBHStockpileWaterBottles` Job，`job.count=min(20,差量)`，由模块重定向为灌 MEE 瓶。
- `Recipe_MEE_BoilWater.xml` ingredient filter 加 `MEE_WaterBottle`：烧水配方现在同时吃生水/瓶水，使灌出的瓶水能被炉灶消费（链路闭合）。
- 注册：csproj 加 `<Compile Include="Source\Comps\WorkGiver_MEE_FillWaterBottles.cs" />`；新建 `Defs/WorkGiverDefs/WorkGiver_MEE_FillWaterBottles.xml`(workType=Haul、priorityInType=210、label 取水供配方)。
- 主工程构建通过（RimHormones.dll）。

### 待办
- 部署需**完全退出 RimWorld**（游戏锁 DLL，运行中 copy 静默失败、内存仍是旧 DLL）。用户退出后跑 `copyToRimWorld.bat` 或 `python D:/RimMods/deploy.py` 再重开验证。
- 验证点：右键设施取水应逐瓶+有耗时、取N出N；炉灶烧水账单在库存无水时应自动派 pawn 去 DBH 设施灌瓶并喂炉灶。
