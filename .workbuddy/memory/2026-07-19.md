# 2026-07-19 工作记录

## 1. 删除失眠(Insomnia) hediff 概念
- 删除目录 `_indexProj/Source/Logic/CortisolLogic/Somnia/`（InsomniaPatch.cs、InsomniaIconPatch.cs）
- 移除 `HediffComp_Cortisol.cs` 中 `IsRestingWell()` 的 Insomnia 状态检查（原 512-514 行）
- 源码已无 Insomnia 引用；XML 描述里"会导致失眠"只是 CortisolNeurasthenia 的说明文字，未动

## 2. 皮质醇(Cortisol)新设计逻辑（用户确认）
严重度档位（3档，替代旧的4档 SleepDeprivation/Normal/ChronicStress/Overload）：

| 区间 | 状态 | 衰减/日 | 冒犯·侮辱权重乘数(发起者) | 神经衰弱概率 | 检测间隔 |
|------|------|---------|------|------|------|
| 0 ≤ S < 0.33 | 正常波动 | 13% | ×0.5 (-50%) | 0% | 6000 Tick |
| 0.33 ≤ S < 0.66 | 承压 | 8% | ×2.0 (200%) | 3% | 6000 Tick |
| 0.66 ≤ S ≤ 1.0 | 高压 | 3% | ×4.0 (400%) | 8% | 6000 Tick |

要点（已与用户对齐）：
- 系数语义 = 直接乘数（100%=不变）
- 作用对象 = 发起互动的 pawn 自己的皮质醇档位
- 删除旧设计：过载耗竭机制(HandleOverloadExhaustion/ApplyOverloadExhaustion/CortisolOverloadExhaustion def)、旧神经衰弱公式 `P=min(0.15+0.03×S×(13-P),0.6)`、4档 enum
- 旧 GetDecay 已是 13/8/3% 每日，无需改
- 神经衰弱改为按档位 0/3/8%，检测间隔从 CortisolDailyCheckInterval(60000) 改为 6000

## 3. 人物交互权重修改（Slight/Insult）
- 机制：`Pawn_InteractionsTracker.TryInteractWithOther` → 遍历 InteractionDef 调 `FinalSelectionWeight = baseSelectionWeight × RandomSelectionWeight`
- 目标：Harmony patch `InteractionWorker_Slight` / `InteractionWorker_Insult` 的 `FinalSelectionWeight`，按发起者皮质醇档位乘 ×0.5/×2/×4
- 基础权重(待 MCP 确认)：Chitchat/DeepTalk/KindWords=0，Slight≈0.05，Insult=0.04
- rimsage MCP 已加入 config(`~/.workbuddy/.mcp.json`) 且用户已在连接器信任并连接成功。但 MCP 工具在会话启动时注入，当前正在运行的会话错过了加载，ToolSearch 仍查不到 `mcp__rimsage__*`。**需用户新开一个会话**，新会话会重新注入工具，届时即可拉真实 InteractionDef 权重。

## 待办
- [x] 用 rimsage MCP 拉真实 InteractionDef 权重（已完成：发现用户贴的"权重"实为 socialFightBaseChance，真实选择权重在 RandomSelectionWeight）
- [x] 实现 Slight/Insult 权重 Harmony patch（按发起者皮质醇档位 ×0.5/×2/×4）—— 已完成 Harmony_CortisolInteraction.cs
- [ ] 实现皮质醇新设计（删旧4档用新3档、删过载耗竭、神经衰弱按档位0/3/8%、检测6000t）—— 已评估范围，待用户确认执行时机

## 5. 神经衰弱(Neurasthenia) hediff 创建（一档）— 已完成
用户给了神经衰弱属性表（一档：可叠加/睡眠效率-33%/红图标/3天/-5心情），要求"创建hediff"。
- **疼痛影响心情机制（rimsage 核实）**：
  - `ThoughtWorker_Pain` 监听 `hediffSet.PainTotal` 返回分级 `ThoughtState.ActiveAtStage`，对应 ThoughtDef 分级 moodPower；`Need_Mood` 累加 `thoughts.TotalMoodOffset()`。
  - 更简单的"沿用"路径：内建 `HediffComp_ThoughtSetter`（Verse）——hediff 加时挂 permanent `Thought_Memory`（`moodOffset=Props.moodOffset`），移除 hediff 时自动清记忆。
- **实现（reuse 现有 `CortisolNeurasthenia` def 改为一档，非新建）**：
  - 新建 `Defs/ThoughtDefs/Thought_Neurasthenia.xml`：ThoughtDef `NeurastheniaMood`（Thought_Memory, nullifiable）
  - `Defs/HediffDefs/Hediff_Cortisol.xml` 的 CortisolNeurasthenia 改写：red `defaultLabelColor (1.0,0.4,0.2)`；stage 仅 `RestFallRateFactor 1.33`（近似 -33% 睡眠效率，pawn 级无直接睡眠效率 stat）；`HediffComp_ThoughtSetter`(thought=NeurastheniaMood, moodOffset=-5)；`HediffCompProperties_Disappears` 180000t(3天)。移除旧的 MentalBreakThreshold/Consciousness/-RestFallRateFactor1.6。
  - 翻译：`Languages/.../DefInjected/ThoughtDef/NeurastheniaMood.xml` + 更新 PhysiqueBodyCondition.xml 的 CortisolNeurasthenia.description
  - 代码 `HediffComp_Cortisol.cs`：① 移除 `TryTriggerNeurasthenia` 的 HasHediff 去重拦截（启用叠加）；② `ApplyNeurasthenia` 时长 GenDate.TicksPerDay*1 → *3
- **叠加语义注意**：多实例时 RestFallRateFactor 会乘性叠加，但 ThoughtSetter 按 thoughtDef 去重 → -5 心情不会叠加（固定 -5）。若需心情也叠加需另写逻辑。
- 注：旧概率公式 CalculateNeurastheniaProbability(0.15/0.03/封顶0.6) 属"旧设计"未动，等"删旧用新"重构时再改 0/3/8%。

## 6. 睡眠机制澄清（rimsage 核实，重要纠正）
- 原版"高效睡眠"特质 = `QuickSleeper`（非字面的 Sleeper），只改 `RestRateMultiplier` stat（基准1.0）加性 offset +0.5→1.5→约2/3时间睡满。Need_Rest.cs:107 用此stat算睡眠恢复。
- `RestFallRateFactor`（hediff字段 restFallFactor / StatDef）控制**清醒时**rest需求下降速度(Need_Rest.cs:115)，>1=更易疲劳。≠"睡眠效率"。
- ⚠️ **CortisolNeurasthenia 当前用的 `RestFallRateFactor 1.33` 与用户要求"睡眠效率-33%"语义错配**（它做的是"更易疲劳"）。正确应为在 stage 用 `statFactors`/`statOffsets` 改 `RestRateMultiplier`=0.67（乘性，-33%恢复效率）。HediffStage 源码(行92/94)支持 statOffsets/statFactors 列表。
- 待用户拍板：是否改正（删 RestFallRateFactor 1.33 + 加 RestRateMultiplier 0.67）。若用户本意就是"更易疲劳"，则保持不动。

## 7. 四大应激源增长皮质醇（已实现）
用户要求：饥饿/环境差/疼痛/心情差 都增长皮质醇。参考原版思想机制。
- 判定方式（rimsage 核实）：
  - 饥饿：`pawn.needs.food.CurLevel < 0.2`（原版 NeedFood 思想，饿-6起）
  - 环境丑陋：`(int)pawn.needs.beauty.CurCategory <= (int)BeautyCategory.Ugly`（枚举 Hideous=0/VeryUgly=1/Ugly=2/Neutral=3；原版 NeedBeauty 丑陋-10）
  - 疼痛：`pawn.health.hediffSet.PainTotal > 0`（原版 ThoughtWorker_Pain 监听同一 PainTotal）
  - 心情差：`pawn.needs.mood.CurLevel < 0.3`
- **修复bug**：旧 GetLowMoodGrowth 用 `CurLevel*100 < -10` 判定，CurLevel 恒≥0 永不触发→已改为 `CurLevel < 0.3`。
- 改动文件：
  - `Define.cs`：删 CortisolChronicStressGrowth；新增 CortisolHungerGrowth=0.002 / CortisolUglyEnvGrowth=0.0015 / CortisolPainGrowth=0.002（CortisolLowMoodGrowth=0.0015 保留）；新增 CortisolHungerThreshold=0.2；CortisolMoodLowThreshold -10→0.3。
  - `HediffComp_Cortisol.cs`：删 GetChronicStressGrowth（原查 food<0.3 或 低温<5°，低温已废弃不在新spec）；新增 GetHungerGrowth/GetUglyEnvGrowth/GetPainGrowth；修正 GetLowMoodGrowth；ProcessCortisolTick 累加四源（可叠加相加）；LogCortisolUpdate/DetermineScene 更新为四源。
- 增长值为每秒×60tick。四源可叠加（同时命中则增长相加）。均受体魄修正系数乘算（change *= physiqueMod）。

## 4. 权重 patch 实现（rimsage MCP 数据驱动）
- rimsage 工具已加载（search_source/read_file/get_def_details/search_defs）。注意：MCP 工具在会话启动时注入，信任后当前会话会自动加载（无需新开会话）。
- 真实数据纠正：InteractionDef 无 selectionWeight 字段；用户贴的 0/0.5%/4% 实为 socialFightBaseChance（Slight=0.005, Insult=0.04，其余默认0）。
- 真实互动选择权重在 worker.RandomSelectionWeight()：Chitchat=1.0, DeepTalk=0.075×好感, Slight=0.02×负因子, Insult=0.007×负因子, KindWords=0.01(仅Kind特质)/0。
- 用户确认：只改 Slight/Insult 的 RandomSelectionWeight，按发起者皮质醇档位 ×0.5(正常波动0-0.33)/×2(承压0.33-0.66)/×4(高压0.66-1.0)。
- 已新建 `_indexProj/Source/Harmony_CortisolInteraction.cs`：CortisolInteractionUtility.GetSlightInsultWeightMultiplier + 两个 [HarmonyPostfix] patch（InteractionWorker_Slight/Insult.RandomSelectionWeight，乘系数

## 8. 生成皮质醇功能测试用例
- 产出 `Tests/皮质醇功能测试用例.md`：覆盖浓度循环/衰减分档/四大应激源/神经衰弱/交互权重/肾上腺素联动/过载耗竭，含前置-步骤-预期-验证点-关联代码。
- 文档第8章明确列出「需求vs当前代码」偏差：旧4档枚举(0.15/0.5/0.75)未删除、过载耗竭机制未删、神经衰弱概率公式未改0/3/8%、检测间隔未改6000、睡眠效率语义错配(RestFallRateFactor 1.33≠RestRateMultiplier 0.67)。
- ⚠️ 额外发现疑似单位错位：增长源×60(每60tick周期) vs GetDecay每tick却每周期只减一次 → 浓度可能过快累积，建议单独做平衡验证。

## 10. 查询：RimWorld Need 能否修改颜色（rimsage 核实）
- 结论：**原生 XML/NeedDef 无法改 need 条颜色**。`Need.cs` 与 `NeedDef.cs` 均无 color 字段；`Need.DrawOnGUI` 调 `Widgets.FillableBar(rect, pct)`，填充用统一贴图 `BarFullTexHor` + 当前 `GUI.color`（默认白），所有 need 同色。
- **代码层可改**：路子A=自定义 Need 子类重写 DrawOnGUI，调 FillableBar 前设 `GUI.color=色` 画完复原（注意阈值线/分隔线会一起被染）；路子B=Harmony 后补丁 `Need.DrawOnGUI` 按 def.defName 判断染色。
- 模组上下文：Cortisol 当前是 Hediff（非 Need），无进度条；若想"需求条式彩色条"需改做 Need；若只想标签变色用 Hediff 的 `defaultLabelColor`（神经衰弱已用此法）。

## 9. 修复 XML 加载错误（ThoughtDef nullifiable）
- 报错：`<nullifiable>true</nullifiable> doesn't correspond to any field in type ThoughtDef` → 游戏无法 LoadAllActiveMods。
- 根因：`Thought_Neurasthenia.xml` 里多写了 `<nullifiable>true</nullifiable>`，ThoughtDef 无此字段（那是其它 Def 类型的字段）。
- 修复：删除该行。`HediffComp_ThoughtSetter` 经 rimsage 核实机制为：TryAddMemory 时 `thought_Memory.permanent=true` 且 `thought_Memory.moodOffset = Props.moodOffset`（-5），故 -5 来自 comp 配置、不依赖 ThoughtDef 字段，删掉 nullifiable 不影响功能。

## 10. Need_Cortisol UI 染红（整体红）
- 用户已自建 `Need_Cortisol : Need`（在 `_indexProj/Source/Needs/Need_Cortisol.cs`，NeedDef 在 `Defs/NeedDefs/Need_Cortisol.xml`，needClass=Hormones.Need_Cortisol；MaxLevel=100，threshPercents={0.33,0.66}，有 GetSeverity()/GetCortisolLevel()）。
- 实现：在 Need_Cortisol 内重写 `DrawOnGUI`，用 `GUI.color = BarColor(0.90,0.30,0.25)` 包裹 `base.DrawOnGUI` 调用，画完复原。效果=整体红色（标签+填充条），阈值刻度线因 DrawBarThreshold/DrawBarDivision 内部自重置 GUI.color 而保持白色可读。Border(BlackTex) 乘红仍黑，bg 乘红变暗红，fill(BarFullTexHor) 乘红变红。
- 因是自有子类，直接 override 即可，无需 Harmony。补了 `using UnityEngine;`。
- 用户本次明确要求"整体红色"，故常量固定红、未做档位渐变；若要随严重度变深浅，改 GetBarColor() 按 0.33/0.66 返回不同红即可。

## 11. 设计拍板进度（2026-07-19 下午）
- **已确认：皮质醇唯一载体 = Need_Cortisol**（旧 Hediff 驱动路径 `HediffComp_Cortisol.CompPostTick → CortisolLogic.ProcessCortisolTick` 应停用/删除，修"双重驱动"bug）。
- **待用户拍板①：神经衰弱 Hediff 以 §一 还是 §六 为准？**
  - §一（已实现）：3天 / -5心情 / 可叠加 / -33%睡眠 / 无恶性循环 / 触发 0/3/8%@6000tick
  - §六（旧版）：1天 / -8心情 / 睡眠中+0.005恶性循环 / +40%触发加成 / 旧公式 min(0.15+0.03×S×(13-P),0.6)@每日
- **待用户拍板②：是否实现 S₀回归 + 安乐跌落 + 睡眠机制？**
  - S₀=0.33 回归：皮质醇向 0.33 磁吸（与已定"按档位固定衰减 13/8/3%"是不同模型）
  - 安乐跌落：四输入源全最优时向 0 跌 -0.30/日
  - 睡眠机制：睡眠中正常 -0.015/秒、高压失眠 +0.005/秒、醒来心情+2、高压>1天触发神经衰弱
- **已拍板②（睡眠部分）：睡眠不减少皮质醇**。用户明确要求"睡眠减少 去除 / 睡眠暂时不减少皮质醇"。
  - 核查结论：当前代码本就无活睡眠消皮质醇逻辑（`Need_Cortisol.NeedInterval` 只做档位衰减+应激源增长；旧 `IsRestingWell` 与 `CortisolRestDecay`/`CortisolGoodRestThreshold` 均为死代码未被调用）。
  - 已清理：删除 `Define.CortisolRestDecay`、`Define.CortisolGoodRestThreshold`、`HediffComp_Cortisol.IsRestingWell`（均为睡眠消皮质醇相关死符号）。
  - 推论：文档§六"睡眠机制"(-0.015/秒正常睡眠消退、+0.005/秒高压失眠、+2醒来心情) 不采用；回归/安乐亦未要求 → 动态机制维持现状（按档位固定衰减 13/8/3% + 四应激源 + 基础随机增长）。
