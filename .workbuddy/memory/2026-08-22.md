# 2026-08-22

- copyToRimWorld_F.bat 闪退修复：Write 工具生成的 .bat 是纯 LF 行尾，Windows 批处理必须 CRLF。LF 导致 `if/else` 块里 `)` 被误判 → 立即退出（一闪而过）。用 Python 把 `\n` 统一转 `\r\n` 修复（278 行 CRLF，0 裸 LF）。教训：本环境 Write 出的 .bat/.cmd 需二次转 CRLF 才能双击运行。沙箱禁止 cmd.exe，无法实跑验证。
- 新增 F:\RimHormones\copyToRimWorld_F.bat：默认 DEST_ROOT=F:\steam\steamapps\common\RimWorld\Mods，仍支持 %1 覆盖 Mods 根目录。原 copyToRimWorld.bat 保留 D:\Steam 默认并支持 %1 覆盖。
- copyToRimWorld_F.bat 最终方案：用户要"直接 copy 原版只改输出路径"。最终用 Python 字节级复制 `copyToRimWorld.bat` → `copyToRimWorld_F.bat`，仅把 `D:`→`F:`、`Steam`→`steam`（第12行 DEST_ROOT）。CRLF 原样保留。不再套任何包装/日志。（前几次"三退"根因：①Write 出的 .bat 是纯 LF，须转 CRLF；②外层 `if(){echo ...(RimWorld mod)...}` 多行括号块内 echo 带 `()` 触发 cmd 误判块结束→整段中止。）
- 坑：本环境 Git-Bash heredoc 传 Python 代码时会吞掉反斜杠，导致 `b'D:\\Steam'` / `'D:\\Steam'` 搜索串与实际文件字节对不上（count=0）。改用不涉及反斜杠字面量的字节替换 `data.replace(b'D:', b'F:').replace(b'Steam', b'steam')` 才成功。教训：在 Bash heredoc 里处理 Windows 路径反斜杠，优先用 `b'D:'`/`b'Steam'` 这类无反斜杠切入点，或先 `data.decode('utf-8')` 再 `str.replace` 并核对。
- **【真正根因】copyToRimWorld*.bat "没有正确的输出/跑到 Step4 就停"：bat 在 `if (...) ( ... )` 块内部的 `echo` 文本若含 `(` `)`（即使成对，如 `echo ...(renamed-to-.disabled)...` 或 `echo WARNING:...(locked by RimWorld).`），cmd 会把 `(` 当代码块开始、`)` 当块结束，把其后内容当成多余参数 → 报"此时不应有 X"语法错误，整脚本异常退出（返回255），后续 Step5/6 拷贝永不执行 → mod 不部署。修复：去掉这些 echo 文本里的括号（改为 `,`/`-` 连接）。两个 bat 第160行、第240行都有，已改。用户机直跑验证：RETURNCODE=0，F:\steam\...\Mods\Rim-Hormones 完整部署（含 Assemblies/RimHormones.dll）。教训：bat 的 `if/for` 括号块内 echo 文本严禁出现裸括号；顶层 echo 不受影响。可用 Python subprocess + `cmd /c bat > log` 在沙箱抓真实报错。
- **皮质醇结算明细 UI 精简**（PhysiqueHealthCard.cs DrawCortisolDetail）：用户要求 ①仅显示非零分量（`abs(v)>0.001f` 才 Row，为 0 的如饮品额外衰减+0.0%/日、糖调制+0.0%/日 不显示）；②删除「↑上升项合计」「↓下降项合计」两行；③只保留净变化。编译 0 错误 0 警告，已通过 copyToRimWorld.bat 部署到 D:\Steam\Mods\Rim-Hormones。需重启游戏生效。
- **StackOverflow 崩溃排查（进行中）**：Crash_2026-08-22 两次崩溃均为纯 native 栈（mono_domain_jit_foreach/mono_domain_foreach），无 managed 帧 → 无限递归炸栈。最后日志行 = `[体魄-初始化] 添加了体魄可视化 Hediff` → 崩溃在 AddHediff 下游 Notify_HediffChanged 链。已读完全部 Source 目录所有 patch + getter：StatWorker.Postfix 不调用 GetStatValue（安全）；StrainWorkBlockPatches 只查 hediffSet（安全）；PhysiqueLgc 全部 getter 为纯 switch/lookup（安全）；HediffComp_PhysiqueDisplay.UpdateDisplay 调 Notify_HediffChanged 但有 IsHashIntervalTick(60) 守卫且 lastPhysiqueLevel 去重。递归入口尚未定位——可能需要加 [ThreadStatic] depth counter 探针抓真实调用链。
- **皮质醇「应激增长」拆分明细**（Need_Cortisol.cs + PhysiqueHealthCard.cs）：用户要求把「应激增长」聚合项拆成 10 个源头分别显示，且翻译做好。做法：新增 public `GetCortisolGrowthSources()` 返回 `List<(string label, float perDay)>`（%/日，scale=(150/60000)*2*4=0.02，与 b.growth 同口径，各项之和=growth）；内部 `CalcGrowthSources()` 抽取 10 源判定（复用 GetGrowthPerInterval 逻辑，tick 行为不变）；UI 卡片 DrawCortisolDetail 与需求条 tooltip GetTipString 均改为按 src 循环显示（仅非零）。中文标签：心情低落/环境恶劣/饥饿/疼痛/患病/受辱/食用生食/浑身湿透/舒适度过低/缺乏娱乐。编译 0 错 0 警；D:\Steam 部署成功，F:\steam 因游戏锁 DLL 未覆盖（需退出游戏后重跑 copyToRimWorld_F.bat）。
- **移除「当前压力源」标签块**：应激增长已拆明细后，PhysiqueHealthCard.cs DrawCortisolDetail 末尾的「当前压力源」多行文本标签块（GetCurrentStressors）删除。注意 GetCurrentStressors() 方法本身保留——HediffComp_CortisolStatus.cs:79 仍调用。编译 0 错 0 警；两轮 deploy 均 RETURNCODE=0 且无 DLL 锁警告（游戏已退出，F:\steam 完整覆盖）。
- **结算明细净变化并入标题 + 适配四代谢需求**：①`Section()` 增加可选 suffix/color 参数（右对齐标题行）。②皮质醇 `DrawCortisolDetail` 的「净变化」行移除，改为标题 `结算明细（%/日）` 右侧带 `净 ±X%/日`（ColorForEffect 上红下绿）。③新增 `ColorForLevelChange`（极性相反：等级上升=绿/下降=红），供代谢需求用。④MEE：`Need_MEE_Base` 把原 abstract `FallPerDay` 拆为 `BaseFallPerDay`(自然,abstract) + `extraFallPerDay`(外部)，`FallPerDay` 改为 `BaseFallPerDay + extraFallPerDay`；新增 `MEEBreakdown` 结构 + `GetMEEBreakdown()`（naturalFall/extraFall/NetPerDay）。四个子类 `FallPerDay=>BaseFallPerDay`（糖 0.2、水 0.55、电解质 0.3、蛋白 0.15）。⑤`DrawMetabolicDetail` 在「满足度」后加「结算明细（%/日）」段：自然消耗 + (外部调节 if≠0)，净变化入标题。编译 0 错 0 警；两路径部署 RETURNCODE=0 无锁。
- **修复「吃肉补电解质/蛋白质」**：用户报告吃肉不补电解质+蛋白质，且肉类判定太窄漏检含肉熟食。根因：`_indexProj/Source/Comps/MEEMeatElectrolytesPatch.cs`（Harmony 补丁 `Thing.Ingested` Postfix）①只补 `Need_MEE_Electrolytes`、漏了 `Need_MEE_Protein`；②肉类判定仅 `foodType.HasFlag(FoodTypeFlags.Meat)`——只认生肉，熟食(foodType=Meal)漏检。修复：重写补丁——新增 `IsMeatBased(Thing)` 同时覆盖「foodType 含 Meat 标记」与「CompIngredients 实际食材含肉」；Postfix 内对 `Need_MEE_Electrolytes` 和 `Need_MEE_Protein` 各 `Satisfy(0.15f)`（新增 `ProteinPerMeat=0.15f` 常量）。主工程 0 错 0 警；D:\Steam 完整部署；F:\steam 因游戏锁 RimHormones.dll 未覆盖（需退出游戏后重跑 copyToRimWorld_F.bat）。
- **PhysiqueHealthCard 移除 工作效率/代谢率/饥饿速率 + 加高容器**：①这两类数值（工作效率/代谢率/饥饿速率）已在体魄 Hediff 悬停 tooltip 里展示（`HediffComp_PhysiqueDisplay.cs:94-96`），卡片内属冗余 → 从左栏概览静态行(`DrawSummary` 末段)和右侧体魄详情「属性影响」段(`DrawPhysiqueDetail`)两处删除（共 3+3 行），左栏末段分隔线一并移除；右侧「属性影响」保留 食欲/战斗加成。②容器加高：`HealthTabPhysiqueUI.cs` 的 `ITab_Pawn_Health_FillTab_Patch.Prefix` 读取 `InspectTabBase.size` 后，仅在 `y < 430+120` 时设 `y = 550`（额外 120px），抵消顶部 toggle 条占的 26px、给体魄 UI 更多空间（绝对赋值避免重复累加，也不缩小其它 mod 加高的窗口）。主工程 0 错 0 警；两路径部署 RETURNCODE=0 无锁。
- **容器加高从 +120 改为 +40**：用户反馈 550 太高，改为 `ExtraHeight=40f`（窗口 430→470）。F:\steam 部署时因游戏锁 DLL 未覆盖（需退出后重跑）。
- **体魄选项卡显示经验值**：`PhysiqueHealthCard.cs` 新增 `GetPhysiqueSkill(pawn)`（读 defName="Physique" 的 `SkillRecord`）+ 常量 `PhysiqueXpPerLevel=1000f`（与原版 `SkillRecord.Learn` 内部每级阈值一致）。①左侧概览体魄按钮下加 `体魄经验 {xpSinceLastLevel:F0} / 1000`；②右侧「等级与阶段」加 `当前经验`/`距下一级`/`升级进度` 三行。主工程 0 错 0 警；D:\Steam 部署成功；F:\steam 游戏锁 DLL 未覆盖。
- **肾上腺素加入体魄卡片**：`PhysiqueHealthCard.cs` 新增 `DetailTab.Adrenaline` 枚举项 + 左侧可点击行（迷你条带 0.15/0.5/0.75 三档阈值刻度，始终显示，无 Hediff 时浓度=0%）；右侧 `DrawAdrenalineDetail` 展示「当前状态」（阶段/浓度/净变化每秒/迷你条）+「当前效果」（意识/移动速度/呼吸循环/代谢/近战伤害/闪避/近战命中/视力/听力，休眠时提示无效果）+「体魄与透支」（体魄修正/透支豁免/休息需求）。配色复用 `ColorForLevelChange`（增益绿/惩罚红）。`Languages/ChineseSimplified/Keyed/Hormones.xml` 加 `Adrenaline`/`Adrenaline{Dormant,Low,Medium,High}` 译文。主工程 0 错 0 警；D:\Steam 与 F:\steam 均 RETURNCODE=0 完整部署（游戏未运行，无 DLL 锁）。需重启游戏生效。
- **肾上腺素「当前效果」单位修正**：用户指出当前效果单位不对（不应是 %/日）。根因：`DrawAdrenalineDetail` 的「当前效果」「体魄修正」用错了 `FormatSigned`（末尾带 `%/日`）；而肾上腺素效果是**瞬时百分比乘子**（如 `Define.AdrenalineLow.Consciousness=0.08` → +8%，通过 `stat*=(1+effect)` 或 Hediff capMod 生效），**不是按天变化的速率**。`%/日` 只用于真正按天变化的量（皮质醇结算、代谢需求结算，保留 `FormatSigned`），`%/秒` 用于净变化（保留内联格式）。修复：新增 `FormatPct`（输出 `±X%`，无时间维度），「当前效果」9 项 + 「体魄修正」改调 `FormatPct`；`FormatSigned` 注释补强调仅用于按天速率。另：`RimWorldWin64.exe`(PID 30912) 实际在跑（之前 `tasklist /FI` 过滤器假阴性），F:\steam\Mods\Rim-Hormones\Assemblies\RimHormones.dll 经 MD5 比对 = 旧版（locked by game，复制失败）；D:\Steam 路径 MD5 = 新构建（一致）。F:\steam 须完全退出游戏后重跑 copyToRimWorld_F.bat 才能覆盖。
- **皮质醇新增 3 个应激源**：用户要求把 ①低血糖 ②浑身无力 ③看见尸体 加进皮质醇「上升/增长」逻辑。实现：`Define.cs` 新增 `CortisolGrowthHypoglycemia=250f`/`CortisolGrowthWeakness=250f`/`CortisolGrowthSeeCorpse=150f`（显示值=常量×0.02 → 分别 +5%/日、+5%/日、每具尸体 +3%/日）。`Need_Cortisol.cs` 的 `CalcGrowthSources()` 末尾加三源：低血糖=HasHediff("MEE_Hypoglycemia")；浑身无力=HasHediff("MEE_Weakness")；看见尸体=CountVisibleCorpses()（同图 + 25格内 + `GenSight.LineOfSight` 无遮挡，`pawn.Map.listerThings.ThingsInGroup(ThingRequestGroup.Corpse)`；注 `ThingDefOf.Corpse` 在本构建引用程序集里不存在故用 ThingRequestGroup）。标签：低血糖/浑身无力/看见尸体(或 看见尸体×N 多具时)，自动进结算明细卡与 tooltip（循环已覆盖）。主工程 0 错 0 警；D:\Steam MD5 一致已覆盖；F:\steam 因 RimWorldWin64.exe(PID 33884) 在跑锁 DLL 未覆盖（须退出游戏后重跑 copyToRimWorld_F.bat）。

