# 2026-08-10 工作记录

## MEE 占位贴图本地化（完成早前被打断的任务）
- 1.6 原版贴图打包进资源、无松散 PNG 可 copy。改用纯色块 PNG 生成器 `文档/gen_placeholders.py`（纯 stdlib zlib+struct）。
- 产出 `Textures/Things/Item/MEE/` 下 5 个 PNG：MEE_RawSugar/Salt/ProteinExtract/WaterBottle/GlucoseMash。
- 4 个旧 ThingDef（生砂糖/盐/蛋白提取物/水瓶）texPath 由 `Things/Item/Resource/...` 与原版 `MedicineHerbal` 改指本地 `Things/Item/MEE/...`，不再引用原版路径。

## 新增 MEE 食谱链：土豆/玉米 → 葡萄糖原浆（酿造台）
- 新 ThingDef `MEE_GlucoseMash`（葡萄糖原浆，ResourceBase 常驻 def，texPath 本地）。
- 两个 RecipeDef：
  - `MEE_MakeGlucoseMashFromPotato`：Potato×10 → MEE_GlucoseMash×1
  - `MEE_MakeGlucoseMashFromCorn`：Corn×10 → MEE_GlucoseMash×1
  - 共同：`recipeUsers=Brewery`、workAmount 1800、workSkill Cooking、skillRequirements Cooking≥2、`MEERecipeMarker` 门控（仅模块启用可见）。
- 翻译：ThingDef MEE_Items.xml（中/英）加葡萄糖原浆；新建 RecipeDef/MEE_Recipes.xml（中/英）。
- 全部 11 个 XML 已通过 minidom 格式校验。
- 待定：葡萄糖原浆与 MEE_RawSugar（生砂糖）的关系——葡萄糖原浆是直接糖源，还是原浆→生砂糖的中间产物？另外 MEE 物品的 Ingest→Satisfy 消费逻辑尚未接线。

## 新增盐的配方：石块→盐（切石机）
- `Defs/RecipeDefs/Recipe_MEE_Salt_FromStone.xml`：StoneBlocks 类别（任意石料）×1 → MEE_Salt×5，recipeUsers=Stonecutter（切石机），workAmount 1500、workSkill Stonecutting、需 ≥2 级，带 MEERecipeMarker 门控。
- 翻译加入 RecipeDef/MEE_Recipes.xml（中/英）。XML 已校验通过。
- 至此 MEE 生产链：土豆/玉米→葡萄糖原浆（酿造台）；石块→盐（切石机）。水/蛋白质/糖（生砂糖）的获取方式尚未定义。

## 移除生砂糖（MEE_RawSugar），葡萄糖原浆即糖
- 用户决定：葡萄糖原浆替换糖，现在没有「生砂糖」了。葡萄糖原浆(MEE_GlucoseMash) 成为「糖」需求的唯一来源。
- 删除 `Defs/ThingDefs/Thing_MEE_RawSugar.xml`、`Textures/Things/Item/MEE/MEE_RawSugar.png`；从 ZH/EN `MEE_Items.xml` 移除生砂糖翻译；`文档/gen_placeholders.py` 去掉该 PNG 生成行。
- grep 确认全仓无 RawSugar 残留引用，翻译 XML 校验通过。MEE_GlucoseMash 描述已是"satisfy the sugar need"，无需改。
- 待补：水(WaterBottle)、蛋白质(ProteinExtract) 的获取方式；MEE 物品 Ingest→Satisfy 消费逻辑仍未接线。

## 蛋白质配方拆分为三个独立食谱（烹饪台）
- 用户纠正：牛奶/鸡蛋/任意生肉 **各一个**食谱，并备注"约 50% 饱食损耗"（把食物转成蛋白质提取物的机会成本/损耗，待接线消费逻辑时再平衡）。
- 删除合并版 `Recipe_MEE_ProteinExtract_FromMilkMeat.xml`，新建 `Defs/RecipeDefs/Recipe_MEE_ProteinExtract.xml` 含 3 个 RecipeDef：
  - `MEE_MakeProteinExtractFromMilk`：Milk×10 → MEE_ProteinExtract×1
  - `MEE_MakeProteinExtractFromEggs`：EggChicken×10 → MEE_ProteinExtract×1
  - `MEE_MakeProteinExtractFromMeat`：MeatRaw 类别（任意生肉）×5 → MEE_ProteinExtract×1
  - 三者均 recipeUsers=Stove（烹饪台）、workAmount 1500、workSkill Cooking 需≥2、带 MEERecipeMarker 门控。
- 翻译同步改为 3 条（中/英）；XML 校验通过，旧合并 defName 无残留。
- 至此 MEE 生产链：葡萄糖原浆(土豆/玉米,酿造台)；盐(石块,切石机)；蛋白质提取物(牛奶/鸡蛋/生肉,烹饪台)。仅剩水(MEE_WaterBottle) 无获取方式。

## 糖↔皮质醇双向联动（事件解耦架构）
- 主 mod 新增静态事件中心 `Source/Metabolic/NeedChangeEvents.cs`：`OnStrainChanged`/`OnCortisolChanged`/`OnSugarChanged`（public 订阅 / internal 触发），回调 (Pawn, oldValue, newValue)。已在主 csproj 加 Compile Include。
- 触发点：Need_Cortisol.CortisolTick 末尾（加 beforeLevel 基准+调制后 clamp+FireCortisolChanged）；Need_MuscleStrain 的 AddStrain 与 NeedInterval；Need_MEE_Base.NeedInterval 末尾调虚方法 OnFallApplied，Need_MEE_Sugar 重写它 FireSugarChanged。
- 主 mod 给模块留的公共回写接口（避免字段入侵）：`Need_Cortisol.SetSugarCortisolModulation(perDayPercent)`（独立通道，不与饮品拓展 extraCortisolDecay 冲突）+ `Need_MEE_Base.SetExtraFallPerDay(f)`（叠加糖消耗速率）。
- 模块 `_metabolicEssentialsExtendedProj/Source/MetabolicLogic_Sugar.cs`：Register() 订阅 OnCortisolChanged+OnSugarChanged，Recompute() 读当前皮质醇严重度与糖满足度，写回双向调制。Init 中 MetabolicLogic_Sugar.Register() 调用。已加模块 csproj。
- 逻辑：皮质醇>20% 时，糖>33%→抑制皮质醇增长 10%/日(SetSugarCortisolModulation(+10))；糖<33%→催高 13%/日(SetSugarCortisolModulation(-13))；皮质醇>20%→糖消耗降为 30%/日(SetExtraFallPerDay(-0.1))；低于阈值调制清零。
- 两工程 `dotnet build -c Release` 均 0 警告 0 错误；模块 DLL 产出至 mod 根 MetabolicEssential/MetabolicEssential.dll。

## 文件重组（用户要求 2 点）
- ① 事件中心 `NeedChangeEvents.cs` 不应归属 Metabolic：从 `Source/Metabolic/` 移至主工程根 `Source/NeedChangeEvents.cs`（属主体通用机制，namespace 仍为 Hormones）。已同步 csproj 编译路径。
- ② 5 个冗余 Metabolic 文件合并为 1 个：删 `IMetabolicModule.cs`/`MetabolicLoader.cs`/`MetabolicBootstrap.cs`/`MetabolicState.cs`/`MEERecipeMarker.cs`，新建 `Source/Metabolic/Metabolic.cs`（同一 namespace Hormones，公开类型名/签名不变，模块工程无感）。csproj 删 5 加 1。`RecipeDef_AvailableNow_MEE_Patch.cs` 仍单列。
- 两工程重新编译均 0 错 0 警。

## 合并 Metabolic.cs → 单一类 MetaBolicLoadCtrl
- 用户要求把 `Source/Metabolic/Metabolic.cs`（内含 MetabolicState/MetabolicLoader/MetabolicBootstrap 三静态类）整合成单一类 `MetaBolicLoadCtrl`。
- 新建 `Source/Metabolic/MetaBolicLoadCtrl.cs`：`[StaticConstructorOnStartup] public static class MetaBolicLoadCtrl`，合并状态标志(Active/IsLoadedMME)+加载(TryLoad/IsModulePresent/ModulePath)+启动触发(静态构造器调 TryLoad)。`IMetabolicModule` 接口 与 `MEERecipeMarker`(DefModExtension) 因 C# 继承约束保留为同文件内的独立类型。
- 删除 `Metabolic.cs`；csproj 的 Compile Include 改名。
- 全仓改名引用：Need_MEE_Base(`MetabolicState.`→`MetaBolicLoadCtrl.`×2)、RecipeDef_AvailableNow_MEE_Patch(×1)、RimHormonesMod(`MetabolicLoader.`→`MetaBolicLoadCtrl.`×2，并顺手修过时文案 Assemblies→MetabolicEssential\ 子目录)、模块工程源码注释+csproj 注释、copyToRimWorld.bat 注释。
- 两工程 `dotnet build -c Release` 均 0 错 0 警；grep 确认 .cs/.csproj 无 `MetabolicState|MetabolicLoader|MetabolicBootstrap` 残留（仅新文件注释作历史说明）。

## 新增：劳损 → 糖消耗 联动
- 用户要求：在执行劳损时减少糖 Need 约 20%/日。
- 关键语义确认：`Need_MuscleStrain` 实为**劳损储备**（CurLevel 高=状态好、低=已劳损；AddStrain 是 CurLevel-= 训练消耗；受伤/精疲 Hediff 由"储备跌破阈值"触发，见 Hediff_StrainPool.xml:650）。故"执行劳损"= 训练动作（储备被消耗），不能静态按 CurLevel 高低判。
- 实现（模块 `_metabolicEssentialsExtendedProj/Source/MetabolicLogic_Sugar.cs`，主工程零改动）：订阅 `OnStrainChanged`；回调里 `newVal<oldVal`（=AddStrain 训练）时把该 pawn 的"劳损生效窗口"刷新为 `Find.TickManager.TicksGame + 3000 tick(≈50s)`；`Recompute` 窗口内对糖额外消耗 `SetExtraFallPerDay(+0.20)`。与皮质醇项合并写回（统一叠加量：皮质醇>20% 贡献 -0.10，劳损窗口 +0.20）。
- 编译：模块 `dotnet build -c Release` 0 错 0 警（曾漏 using System.Collections.Generic 导致 Dictionary 报错，已补）。

## 糖基础自掉速率改为 10%/日
- 用户要求：糖每日自己掉落约 10%。原基础 40%/日（我此前自选的占位值）改为 10%。
- 两处同步改：`Need_MEE_Sugar.FallPerDay` 的 `0.4f`→`0.1f`（主工程实际消耗基准）；模块 `MetabolicLogic_Sugar.SugarFallBase` 0.40→0.10（偏移计算基准，保持与需求一致）。
- 新组合（糖消耗/日）：静息无皮质醇=10%；仅皮质醇>20%=30%；仅劳损中=30%；皮质醇>20%且劳损中=50%。两工程重新编译 0 错 0 警。
