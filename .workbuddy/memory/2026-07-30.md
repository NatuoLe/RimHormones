
## 修复「体魄热情倍率覆盖」bug（2026-07-30）
- 需求：把 Physique 技能的热情学习倍率从原版 无0.35/好奇100%/狂热150% 改为 无100%/好奇110%/狂热120%。
- 发现 `HormonesLogic.cs` 的 `SkillRecord_Learn_Physique_Patch`（Prefix patch `SkillRecord.Learn`，仅对 Physique 生效）早已存在，自定义倍率也已是 1.0/1.1/1.2，但一直没生效——根因：`GetOriginalPassionMultiplier` 原版倍率写错成 1.0/1.5/2.0/4.0，导致补偿算式 `xp/原版*自定义` 分母错误。
- **RimWorld 1.6 原版真实 passion 倍率**（灰机 wiki SkillDef.json PassionsData.LearningFactor）：无=**0.333**、好奇=1.0、狂热=1.5（不是 0.35！）。已据此修正 GetOriginalPassionMultiplier。
- **后续（当日追加）**：用户最终决定不纠结 0.333，直接硬编码分母 `无0.35 / 好奇1.0 / 狂热1.5`。已把 `GetOriginalPassionMultiplier` 三档改为 0.35/1.0/1.5，编译 0 错 0 警，DLL 已 cp 到 Steam（MD5 04767c8b…一致）。放弃了此前的反射探针方案（读 SkillDef.PassionsData 运行时倍率）。
- 另修一个副作用 bug：Prefix 会作用于所有 Physique 的 Learn 调用，包括日常衰减的 `Learn(-decayXP,direct:true)`，会把衰减量按 passion 缩放失真。已在 Prefix 加 `if (xp <= 0f) return;`，热情倍率只作用于正经验学习。
- 确认三处正向 Learn 均 direct:false（原版会乘passion），补偿算式成立。编译0错0警，DLL 已 cp 到 Steam（MD5校验一致 334ea5c2…）。
- **待办**：Excel `Dev/体魄系统数值一览.xlsx` 已加「热情倍率覆盖」表(Sheet8)的生成代码，但因 WPS(et.exe)占用文件保存失败，需用户关闭后重跑 `Dev/gen_physique_xlsx.py`。

## 实现「体魄日常衰减」功能（用进废退）（2026-07-30）
- 需求：给体魄加日常衰减，用户确认三项设计——机制=扣Physique技能经验、豁免=劳作或锻炼任一、强度=温和。
- 数值（PhysiqueDefine.cs 新增）：`DecayTicksPerDay=60000`；阶段日衰减XP `Frail=0/Average=2/Fit=4/Strong=8/Peak=14`；`DefaultPhysiqueDecayGlobalMult=1`（玩家可调0~3，0=关）。
- 实现要点：
  - `PhysiqueLgc.GetDailyDecayXP(pawn)` 按阶段返回衰减XP（权威来源约定）。
  - `HormonesComponent` 加字段 `decayTickAccumulator`(int)+`activeToday`(bool)，均已 Scribe 持久化；`CompTick` 里 `PhysiqueDecayTick()` 累计满一天触发 `DailyPhysiqueDecay()`：仅类人(IsHormoneSubject)、activeToday则跳过、globalMult<=0跳过、虚弱XP=0跳过；**扣减用 `rec.Learn(-decayXP, direct:true)`**（负经验+direct避免乘学习速率，内部自动处理掉级）。结算后 activeToday=false。
  - 新增 public `MarkActivityToday()`：在 `PhysiqueWorkSettle.SettleWork` 开头 + `JobDriver_Exercise` tickAction 里调 `pawn.GetComp<HormonesComponent>()?.MarkActivityToday()` 打标记。
  - Settings 加 `PhysiqueDecayGlobalMult` 滑块(0~3)+持久化。
- 编译0错0警，DLL已cp到 `D:\Steam\...\Mods\Rim-Hormones\Assemblies\`。**未动 AdrenalineProducer/AdrenalineLogic/HormonesLogic public 签名（只加了新public方法），CE补丁包无需重编译。**
- Excel 更新：`Dev/体魄系统数值一览.xlsx` 增加第7表「日常衰减」（阶段衰减表+机制要点表），脚本 `Dev/gen_physique_xlsx.py` 同步。

## 输出「体魄系统数值一览.xlsx」文档（2026-07-30）
- 生成 `Dev/体魄系统数值一览.xlsx`（脚本 `Dev/gen_physique_xlsx.py`，openpyxl/同 venv），6 工作表：概览/阶段-身体属性(生效)/阶段-系统内部修正/等级线性修正/成长-经验获取/特质偏移。五阶段配色。
- 数据源：`PhysiqueDefine.cs`(常量) + `PhysiqueLgc.cs`(阶段switch值,注意与XML用途不同) + `Hediff_PhysiqueDisplay.xml`(游戏真实生效capMods/statFactors/statOffsets/hungerRateFactorOffset) + `ExerciseWork.cs`(锻炼5000tick/0.075XP每tick/劳损0.08每tick/35%门槛) + `Trait_PhysiqueAptitudes.xml`(特质偏移)。
- **关键区分**：`Hediff_PhysiqueDisplay.xml` 是游戏内对pawn真实生效的属性来源（按severity映射阶段）；`PhysiqueLgc.cs` 的 GetWorkEfficiency/GetHungerRate 等 switch 值是 mod 内部激素/劳损逻辑用的，两者数值不完全一致，Excel 已分两表列出。等级线性：恢复加成1.0→1.5、伤害减免1.0→0.5。阈值型：肾上腺素<8×0.5/≥13豁免；皮质醇<8×0.5/≥13×1.2。

## 输出「皮质醇数值.xlsx」文档（2026-07-30）
- 生成 `文档/Hediff/皮质醇数值.xlsx`（openpyxl，venv 同上），8 工作表：总览/档位阈值与效果/衰减来源/增长来源/体魄修正/心情效果/相关Hediff/结算公式。绿褐主色以区别肾上腺素表。
- 数据源：`Need_Cortisol.xml`(needClass/minIntelligence=ToolUser) + `Need_Cortisol.cs`(MaxLevel=10000, NeedInterval 150tick 驱动 CortisolTick, 衰减/增长/体魄修正 三项 ×5, threshPercents 0.33/0.66) + `Define.cs`(衰减/增长常量) + `Thought_CortisolMood.xml`/`Thought_Neurasthenia.xml`(心情体魄分档) + `Hediff_Cortisol.xml`/`Hediff_GoodSleep.xml` + `Harmony_CortisolInteraction.cs`(冒犯权重×0.5/2.0/4.0)。
- **关键：以代码实际值为准（Define.cs 与旧注释有出入）**：基础衰减 正常13%/承压10%/高压10%（注释写8%/3%已过时）；增长 低心情+12%/环境+6%/饥饿+10%/疼痛+8%/得病+8%/被辱+3%；额外衰减 高心情-10%/美食-8%/优质睡眠-13%/娱乐-35%。神经衰弱 承压3%/高压8% 每6000tick检测。射击透支倍率现为 0.1（Define.cs AdrenalineRangedOverexertChanceMultiplier）。

## 输出「肾上腺素数值.xlsx」文档（2026-07-30）
- 生成 `文档/Hediff/肾上腺素数值.xlsx`（openpyxl，venv=C:\Users\zhou\.workbuddy\...\envs\default），7 工作表：总览/浓度档位阈值/Hediff阶段能力修正/生成与衰减公式/战斗档位效果/体魄修正与豁免/透支损伤触发。
- 数据源：`Hediff_Adrenaline.xml`(4阶段capMods) + `Define.cs`(AdrenalineLow/Medium/High + 阈值/衰减常量) + `AdrenalineProducer.cs`(生成/衰减公式) + `AdrenalineLogic.cs`(透支抽取三段) + `PhysiqueDefine.cs`(惩罚阈值8/豁免阈值13/惩罚系数0.5)。
- 关键数值固化：生成倍率=1.8-0.04×体魄；透支概率=(0.05+0.04×(13-体魄))×倍率(近战1.0/射击0.5)；档位阈值0.15/0.5/0.75；受击生成0.10为主来源、近战0.03、射击0；衰减=0.02+0.003×体魄。
- 坑：heredoc 传含中文+引号的 Python 脚本会 bash 解析失败（unexpected EOF）→改为 Write 脚本文件再执行，中文用 \\u 转义更稳。

## 锻炼消耗劳损 + 35% 体力储备门槛（2026-07-30）
- 关键认知：「劳损」= `Need_MuscleStrain`（劳损储备 Need）。`CurLevel` 越高越有力，干活 `AddStrain(x)` 是**扣**（CurLevel-=x），睡觉/娱乐(joyKind!=null)在 NeedInterval 回复。CurLevel/MaxLevel 即体力储备百分比。MaxLevel 按体魄档 650~3000。
- 澄清：原版研究台**没有**「体力/技能低于多少不能用」的限制；用户说的「参考研究台」实际是要 WorkGiver 条件不满足→不派活+悬停提示的机制。用 `JobFailReason.Is("...")` 实现。
- 改动（`ExerciseWork.cs` + `PhysiqueDefine.cs`）：
  - Define 新增 `ExerciseStrainPerTick=0.08f`（×体魄消耗倍率；一般体魄5000tick疗程约扣400≈40%储备）、`ExerciseMinStrainPct=0.35f`。
  - `WorkGiver_Exercise.HasJobOnThing`：储备 CurLevel < MaxLevel*0.35 → `JobFailReason.Is("体力不足，无法锻炼（劳损储备低于 35%）")` + return false（右键/悬停可见）。
  - `JobDriver_Exercise.tickAction`：每 tick `strain.AddStrain(ExerciseStrainPerTick * GetMuscleStrainConsumeMultiplier(pawn))`；练到跌破 35% 立即 `EndJobWith(JobCondition.Succeeded)` 停下，避免耗尽储备。
  - ExerciseWork.cs(namespace Hormones.Jobs) 加 `using Hormones.Logic.PhysiqueLogic;` 才能用 PhysiqueLgc；Need_MuscleStrain/Define 在外层 Hormones 可直接解析。
- 编译 0 错误；游戏运行中(RimWorldWin64 PID13944)锁定 DLL **部署失败**，待关游戏后 cp bin/Release/net48/RimHormones.dll → Steam Mods/Rim-Hormones/Assemblies/。（纯 C# 改动，未动 public 签名，不影响 CE 包）

## 新增「锻炼」工作 + 「简易锻炼点」建筑（练体魄技能）
- 需求：新增独立锻炼 WorkType + 杂项建筑物简易锻炼点，殖民者去锻炼点锻炼给 Physique 技能加经验（借用原版贴图占位）。
- 新增 4 个 XML（均按 1.6 结构）：
  - `Defs/WorkTypeDefs/WorkType_Exercise.xml`：WorkTypeDef `Hormones_Exercise`，naturalPriority=90(低)，relevantSkills=Physique，workTags=ManualDumb。
  - `Defs/ThingDefs/Thing_ExerciseSpot.xml`：ThingDef `Hormones_ExerciseSpot`(ParentName=BuildingBase)，1x1，texPath 借用 `Things/Building/Furniture/Dresser` 占位，costList WoodLog45+Steel10，designationCategory=Misc，`researchPrerequisites Inherit="False"`(无科技前置)，building.isEdifice=false。
  - `Defs/JobDefs/Job_Exercise.xml`：JobDef `Hormones_DoExercise`，driverClass=`Hormones.Jobs.JobDriver_Exercise`。
  - `Defs/WorkGiverDefs/WorkGiver_Exercise.xml`：WorkGiverDef `Hormones_DoExercise`，giverClass=`Hormones.Jobs.WorkGiver_Exercise`，workType=Hormones_Exercise。
- 新增 C#：`Source/Logic/PhysiqueLogic/ExerciseWork.cs`(namespace `Hormones.Jobs`)：
  - `WorkGiver_Exercise : WorkGiver_Scanner`：PotentialWorkThingRequest=ForDef(Hormones_ExerciseSpot)，ShouldSkip 非 Humanlike 跳过，HasJobOnThing 校验 forbidden/CanReserve/CanReach/burning，JobOnThing 产 Hormones_DoExercise。
  - `JobDriver_Exercise : JobDriver`：TryMakePreToilReservations 预约建筑(1人)，MakeNewToils=Goto+Delay(5000t)带进度条，tickAction 每 tick 面朝建筑并 `SkillRecord.Learn(0.075f, false)` 给体魄加经验。
  - **已核实 API(mcp)**：`SkillRecord.Learn(float xp,bool direct=false,bool ignoreLearnRate=false)`；WorkGiver_Scanner 抽象结构；范本 JobDriver_WatchBuilding(Toils_Goto+ToilMaker.MakeToil+Delay)。
  - csproj 显式列表已加 `<Compile Include="Source\Logic\PhysiqueLogic\ExerciseWork.cs" />`。
- 编译 0 警告 0 错误。**4 个 XML 已部署 Steam**，但 DLL 因游戏运行(RimWorldWin64 PID9608)锁定**未部署**，待关游戏后 cp bin/Release/net48/RimHormones.dll → Steam Mods/Rim-Hormones/Assemblies/。

## 激素/损伤系统加物种守卫：仅类人生物生效，动物排除
- 问题：对动物发起近战攻击时，Verb_MeleeAttack.TryCastShot patch 对所有 pawn 生效 → 动物 attacker 走 OnAttack → 加 Adrenaline hediff → 满足 High 档触发 TryApplyOverexertDamage → 在**动物身上**产生身体损伤 Hediff（透支损伤）。用户要求这些损伤只在人身上产生。
- 修复：PhysiqueLgc 新增 public 守卫 `IsHormoneSubject(Pawn)`（`pawn.RaceProps.Humanlike` 判定，非类人一律 false）。在核心入口调用：
  - AdrenalineProducer.OnAttack / OnHit 开头 `if (!IsHormoneSubject) return;`（肾上腺素源头拦截，动物根本不会有 Adrenaline）
  - AdrenalineProducer.ProcessAdrenalineDynamic：非类人直接 RemoveHediff(adrenaline) 清理旧存档遗留
  - AdrenalineLogic.TryApplyOverexertDamage：双保险守卫
  - MuscleStrainUtility.TryAddMuscleStrain：肌肉劳损守卫
- 命名空间：IsHormoneSubject 在 Hormones.PhysiqueLgc；MuscleStrainUtility 在 Hormones.Logic.PhysiqueLogic（外层查找可直接用 PhysiqueLgc）。未改任何现有 public 签名 → **CE 补丁包无需重编**。
- 初始化问题答复：皮质醇/肌肉拉伤 Need 均 minIntelligence=ToolUser，动物是 Animal 智力不满足，初始化时**不会加给动物**；体魄是 SkillDef，动物 pawn.skills==null 不赋予。此前本就安全。
- 编译 0 错误，但游戏运行中(RimWorldWin64 PID37968)锁定 DLL **部署失败**，待关游戏后 cp bin/Release/net48/RimHormones.dll → Steam Mods/Rim-Hormones/Assemblies/。

## 损伤 Hediff 可治疗/不可治疗设计（进行中）
- 需求：9(实为10个def)损伤里一部分可被医生治疗、一部分不可，用户已确认治疗语义=「照料加速恢复」(TendDuration：可治的保留自然时间恢复兜底 + 医生照料后额外加速；不可治的只能时间自愈、点不了照料按钮)。分组由用户逐个指定。
- 当前 tendable 现状：仅 CardioOverexert/SuffocationStrain=true(但无 TendDuration comp，照料无实际效果)，其余 8 个=false。
- 已在文档 `文档/Hediff/损伤Hediff与触发逻辑.xlsx` 的「Hediff总览」表新增 I 列「是否可治疗」(表头+行3-12留空+下拉校验"可治疗/不可治疗"，标题合并扩到A1:I1)，供用户填写分组。
- **决定(2026-07-30)：用户要求先全部不可治疗**。已把 Hediff_StrainPool.xml 里仅有的 2 处 tendable=true(CardioOverexert 心肺透支、SuffocationStrain 缺氧损伤)统一改为 false → 现全部 10 处 tendable=false。纯 XML 改动、不重编 DLL，已 cp 到 Steam Mods/Rim-Hormones/Defs/HediffDefs/。心肺/扭伤拆分方案暂搁置。重启游戏生效。
