# 2026-08-12 工作日志

## 排查 XML 报错：billInsertion / MEE_GlucoseMash1 幻象
- 用户报 `billInsertion` 无效字段（BuildingProperties）+ `MEE_GlucoseMash1`/`Corn`/`Potato` 跨引用失败。
- **根因**：Steam 本地 Mods 副本（`D:/Steam/.../Mods/Rim-Hormones/`）**部署陈旧**——`Thing_MEE_WaterBuildings.xml` 仍残留 2 处 `billInsertion`（源文件已在上轮修掉=0）。游戏跑的是旧副本。
- 用 rimsage 确认 `billInsertion` 在 RimWorld 1.6 源码**完全不存在**（无效字段，删掉即正确）。
- `MEE_GlucoseMash1`（带"1"后缀）+ Corn/Potato 跨引用失败 = `billInsertion` 解析崩溃的**连锁幻象**：全盘（C/D + workshop）搜不到任何 `MEE_GlucoseMash1` 字面量，仅 1 个 `MEE_GlucoseMash` defName，无重名。
- **修复**：直接 `cp -r` 同步源 `Defs/Patches/About/Languages/Config` 到 Steam 副本（绕开 copyToRimWorld.bat 的 dotnet build——本次纯 XML 改动无需重编；注意该 bat 若 build 失败会在 Step5 前 `goto :error` 导致内容不复制）。验证 Steam 副本 `billInsertion`=0、FromCorn 干净。
- **隐患提醒**：`D:/Steam/.../workshop/content/294100/3771856492/` 是工坊订阅的**老版本**（仅 Hediff/Need/Thought 等，无 ThingDefs/RecipeDefs），若与本地 dev 副本同时启用会触发共有 defName 撞车（"1"后缀）。用户需确保只启用一份。

## 1.6 铁律补充
- `BuildingProperties` **无** `billInsertion` 字段；"建筑自动挂账单"在 1.6 无此 XML 开关（需 Comp/代码实现），当前水建筑设计为玩家手动挂账单即可，移除该字段。
