# 2026-07-23 工作日志

## 修复 csproj 源文件路径（23:36，接 07-22 会话）
- 用户把 `ThoughtWorker_CortisolMood.cs` 从 `Source\Needs\` 移到 `Source\Logic\CortisolLogic\`，csproj 仍指旧路径 → 编译报 `CS2001 未能找到源文件`。
- 改 `Assembly-CSharp.csproj` 第27行 `<Compile Include="Source\Logic\CortisolLogic\ThoughtWorker_CortisolMood.cs" />`；命名空间仍 `Hormones`（与 RootNamespace 一致）无需动。
- 教训：挪动/新增 .cs 后必须同步 csproj 的 `<Compile Include>`，否则 CS2001。

## 神经衰弱/优质睡眠获取飘字 + 概率（00:01）
- 需求：确认两 Hediff 添加逻辑正常，并加「获取时飘字 + 概率」。
- 确认添加逻辑本就正常：`ApplyNeurasthenia()` 与 `AddGoodSleepHediff()` 都是 DefDatabase→HediffMaker→Severity=1→AddHediff。
- 改动（Need_Cortisol.cs）：
  - 提炼通用飘字 `ShowMote(string text)`（复用 MuscleStrainUtility 的 MoteText 方式）。
  - 神经衰弱：`TryTriggerNeurastheniaCheck` 每 6000tick roll 时弹 `神经衰弱检测 X%` 飘字；`ApplyNeurasthenia` 触发时弹 `神经衰弱 触发!`。
  - 优质睡眠：`AddGoodSleepHediff` 添加时弹 `优质睡眠 ✓`。
- 排查「看不到检测日志/飘字」：根因是上一轮给飘字加了 `if(probability>0)` 门槛——平静殖民者 cortisol=0→概率0%→飘字永不弹；且日志是不起眼的 `Log.Message`、每6000tick才一次。
- 本次修复：去掉 `probability>0` 门槛（每次 roll 都弹 `神经衰弱检测 0%/3%/8%`），并把检测日志 `Log.Message`→`Log.Warning`（醒目）。
- 验证过部署 DLL：md5 与构建产物一致、`神经衰弱检测/触发/优质睡眠` 字符串均 FOUND、游戏未运行（非热加载旧版）、Mods/Rim-Hormones 是真实目录非软链、项目目录 `Assemblies\RimHormones.dll`(6/22旧) 不被游戏加载。
- 干净重建（0错误）+ 部署（00:01:48）。**需重启游戏**。
- 提示：要看概率>0/触发，需让殖民者 cortisol ≥33%(承压档) 有应激源；平静者只会一直弹 `神经衰弱检测 0%`（确认系统在跑）。

## 神经衰弱改为直接砍休息效率 50%（00:13）
- 用户看截图(休息值下降速率160%=皮质醇×120%×神经衰弱×133%)误以为"睡眠效率增加了"。
- 排查：两个倍率来自 Hediff 的 statFactors——皮质醇 `RestFallRateFactor 1.2`、神经衰弱 `RestFallRateFactor 1.33`（Hediff_Cortisol.xml）。
- 关键澄清：`休息值下降速率`(RestFallRateFactor) 越高=疲劳掉越快=**减益**，160% 是正确的 debuff（易累60%），并非"效率增加"；而用户以为的"睡眠效率"是另一项 `休息效率乘数`(RestRateMultiplier=98%，睡眠时回复速度)，之前两 Hediff 都没碰它。
- 两 stat 均在游戏 Core Stats_Pawns_General.xml 确认合法：RestRateMultiplier(回复速度)/RestFallRateFactor(下降速度)。
- 改动（Hediff_Cortisol.xml CortisolNeurasthenia stage0）：`RestFallRateFactor 1.33` → **`RestRateMultiplier 0.5`**（直接砍休息效率50%，UI「休息效率乘数」显示×50%）。皮质醇×1.2 未动。
- 纯 XML 改动无需重编 DLL，已部署（00:13）。**需重启游戏**。

## 体魄影响心情+皮质醇涨幅 & 修优质睡眠（00:49）
- **关日志**：删除 TryTriggerNeurastheniaCheck 的 Log.Warning（神经衰弱检测），保留概率飘字。流程日志 LogCortisolFlow 的 Log.Message 此前已被删（空壳），仅补 physiqueGrowth 参数保持可编译。
- **修优质睡眠不触发（真bug）**：原 CortisolTick 先 `lastRestLevel=currentRest` 再调 CheckForWakeUp，后者又读同一 currentRest 与之比较→转移条件永假；且 TryApplyGoodSleep 要求 `Rest<0.03`（刚醒者Rest必高）自相矛盾。改法：删 lastRestLevel，加 `wasAsleep` bool，CheckForWakeUp 用 `pawn.jobs.curDriver.asleep`（JobDriver 公共字段）检测 asleep→awake 跳变；TryApplyGoodSleep 删掉 Rest<0.03 条件。GoodSleep hediff 有 Disappears(60000tick=1天)，可每日重触发。
- **体魄系统**：`PhysiqueLgc.GetPhysiqueStage(pawn)` 返回 `PhysiqueStage`（Frail/Average/Fit/Strong/Peak=0..4），均在 `namespace Hormones`，Need/ThoughtWorker 直接调用无需 using。
- **皮质醇心情（含体魄，15档）**：ThoughtWorker_CortisolMood 改为 stage=tier*5+physIdx；Thought_CortisolMood.xml 扩为15 stage（tier基础+2/-2/-5 + 体魄偏移：正常+1~+3、承压-4~-1、高压-8~-2）。
- **神经衰弱心情（含体魄，5档）**：新建 ThoughtWorker_NeurastheniaMood（查 CortisolNeurasthenia hediff + 体魄→stage=physIdx）；Thought_Neurasthenia.xml 改 workerClass+5 stage(-7/-5/-4/-2/-1)，去掉 thoughtClass Thought_Memory；Hediff_Cortisol.xml 删掉 CortisolNeurasthenia 的 ThoughtSetter comp（避免与worker心情叠加），保留 Disappears。
- **体魄影响皮质醇每日涨幅（表C）**：Need_Cortisol 新增 `GetPhysiqueGrowthPerDay(severity)`（3档×5体魄 点数/日×100：如 tier0 frail+200/peak-1000，tier2 frail+500/peak-500），CortisolTick 里 `CurLevel += physiqueGrowth(=*150/60000)`。
- csproj 注册 ThoughtWorker_NeurastheniaMood.cs（在 Source/Logic/CortisolLogic/）。
- 干净重建（0错误）+ 部署 DLL+3XML（00:49）。**需重启游戏**。
