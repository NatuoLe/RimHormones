# 2026-08-13 工作日志

## 重要部署坑：copyToRimWorld.bat 不拷贝 Textures（23:0x）
- 现象：启动报 "Collection cannot init: No textures found at path Things/Item/MEE/..." 与 "Could not load Texture2D ... MEE_WaterWell"。
- 根因（关键）：`copyToRimWorld.bat` 只 xcopy `About/Defs/Patches/Languages/Config` + DLL，**完全不碰 `Textures/`**。源 `Textures/` 有全部 PNG（物品4 + 建筑2），但部署目录 `Textures` 一直是空的 → 所有贴图报错。同理任何纯 XML/贴图改动若只跑 bat，贴图不会更新。
- 修复：手动 `cp -r 源/Textures/. 部署/Textures/`（或 `rm -rf 部署/Textures && cp -r 源/Textures 部署/`）。已验证部署 6 个 PNG 就位，`Defs` 全量 `cp -r` 后源↔部署 `diff` IDENTICAL。
- 铁律补充：改贴图后**必须**单独同步 `Textures/`；bat 不可信赖贴图同步。
- **更正（23:1x）**：5 个 `Thought_MEE_*` 的 "affects mood but doesn't have a description" **不是**旧快照问题（上一轮此判断错误）。真实根因：判定在 `ThoughtStage.ConfigErrors`：`baseMoodEffect!=0 && description.NullOrEmpty()`，这里的 `description` 是**阶段 `<li>` 自身的字段**，不是 ThoughtDef 级 `<description>`。原文件 `<li>` 只写了 `<label>`+`<baseMoodEffect>`，缺阶段级 `<description>` → 报错。修复：在每个 `<li>` 内补 `<description>`（复用 def 级文案即可）。⚠️ 以后写 mood thought 必须把 description 写进 stage，否则必报此错。
- **贴图另一根因（23:1x）**：4 个 MEE 物品 `<graphicClass>Graphic_StackCount</graphicClass>` 是**集合图形**，加载时找 `texPath_0/_1/...` 编号贴图；占位 PNG 是单张 → "Collection cannot init: No textures found"。修复：把 4 个物品 `Graphic_StackCount`→`Graphic_Single`（单 PNG 即可，占位够用）。⚠️ 凡用 `Graphic_StackCount`/`Graphic_Random`/`Graphic_Flicker` 等集合类，必须提供编号变体或精灵表，否则必报 collection 错误。operatorname

## 修复：MEE HediffDef/ThoughtDef 一批 XML 根元素与字段错误（22:4x-22:5x）
- 现象：游戏启动报 "root element named X; should be named Defs"、`<listable>`/`<showWhenInitialized>` 非 HediffDef 字段、comp 类名错、`ArgumentNullException parameter s`。
- 根因与修复：
  - 4 个 `Defs/HediffDefs/Hediff_MEE_*.xml` 与 5 个 `Defs/ThoughtDefs/Thought_MEE_*.xml` 根元素写成 `<HediffDef>`/`<ThoughtDef>`，需包 `<Defs>`。
  - `<listable>`、`<showWhenInitialized>` 均**不是** `HediffDef` 字段（已 rimsage 核对 Verse/HediffDef.cs 确认），已删除。
  - comp 类名写成 `HediffComp_Disappears`（运行时类），应为属性类 `HediffCompProperties_Disappears`（已核对源码存在）。
  - `ArgumentNullException` 真凶：`WaterPoisoning` 的 statOffsets 写 `<stat>WorkSpeed</stat>` —— 该 StatDef **不存在**，正确名是 `WorkSpeedGlobal`（已 rimsage 核查）。改用 `WorkSpeedGlobal` 后消失。
- 已 `cp -r` 同步 Steam 副本 4 个 HediffDef + 5 个 ThoughtDef；Python 全量扫描源与部署两份 Defs，确认无残留 root/字段/类名错误。
- 铁律补充（HediffDef 写法）：① 文件根 `<Defs>`；② 仅用 HediffDef 真实字段（无 listable/showWhenInitialized）；③ comp 用 `*CompProperties_*` 属性类；④ stage 内 statOffsets 的 `<stat>` 必须是存在的 StatDef（效率=WorkSpeedGlobal）。

## 修复：`MEE_GlucoseMash1/2/5` 跨引用报错（products 写法根因）
- 现象：改 `<products>` 的 `<count>` 从 1→2→5，报错后缀同步变 `MEE_GlucoseMash1/2/5`。
- 根因（rimsage 查 XmlHelper.ParseElements + ThingDefCountClass）：`products` 写成 `<li><thingDef>MEE_GlucoseMash</thingDef><count>N</count></li>` 是错的。`xmlRoot.Name=="li"` 分支用 `<li>` 的 InnerText（"MEE_GlucoseMash"+"N"）当 thingDef 名注册跨引用，于是去找 `MEE_GlucoseMashN` 这个不存在的 def。
- 之前误判为"重复加载/与 products 无关"——**错误结论**，已纠正 MEMORY.md 的 products 铁律段。
- 正确写法：简写 `<MEE_GlucoseMash>N</MEE_GlucoseMash>`。
- 修复范围：5 个配方全部改简写——FromCorn(5)、FromPotato(1)、ProteinExtract×3(1)、Salt(5)、WaterBottle(10)。已 `cp -r Defs` 同步到 Steam 副本并验证。
- 验证：Steam 副本 5 个 recipe 的 products 均为简写，无残留 `<li><thingDef>`。

## 复查：Corn/Potato/Stove/EggChicken 跨引用报错（21:5x）
- 现象：报 Corn/Potato/Stove/EggChicken 找不到（wanter=thingDefs / recipeUsers）。
- rimsage 核实：`RecipeDef.recipeUsers`=`List<ThingDef>`(RecipeDef.cs:76)，`IngredientCount`=filter(ThingFilter)+count。本项目 5 配方的 recipeUsers(<li>Stove</li>) 与 ingredients(filter+thingDefs) 写法**完全标准，无需修改**。
- 根因判定：这批报错是**修复前** products 错误写法（`<li><thingDef>..</thingDef><count>..</count></li>`）连锁崩溃产物——错误写法使 RecipeDef 整体解析失败，其体内引用的所有原版 def（Corn/Potato/Stove/EggChicken）跨引用全部悬空失败。现已全改简写并同步 Steam 副本，彻底重启后应消失。
- 注：EggChicken 属 Biotech DLC；若用户未启用 Biotech，ProteinExtractFromEggs 会**真正**报 EggChicken 缺失（独立问题，需确认 DLC 启用）。

## 诊断：Corn/Potato/Stove 跨引用报错（22:1x，第N次复发）
- 用户多次报 `Could not resolve cross-reference to Verse.ThingDef named Corn/Potato/Stove`。本轮彻底排查：
  - 源 + Steam common 副本 5 配方文件全部 XML 格式良好（Python ET 解析 ALL WELL-FORMED）。
  - 全盘（D:+C:）仅 1 份 Rim-Hormones（`Mods/Rim-Hormones`），源与 Steam 副本一致且干净；工坊 3771856492 无 RecipeDefs；C 盘无任何副本；无 `<thingDef>MEE` 坏写法残留。
  - rimsage 核实 1.6：`RecipeDef.recipeUsers=List<ThingDef>`、`ingredients=List<IngredientCount>`(filter+count)、`products=List<ThingDefCountClass>`；`IngredientCount` 无自定义 LoadDataFromXmlCustom，filter/count 为标准字段。本 mod 全部写法与源码一致、正确。
- 结论：**磁盘上本 mod 文件 100% 干净且语法权威正确，唯一副本亦干净 → 当前文件不可能产生该报错。** 用户持续看到的报错来源只可能是 (a) 修复前的旧日志累积（尤其若只"回主菜单重载存档"而未彻底杀进程，游戏内调试日志不清空）；(b) 游戏从沙箱视野外的另一份安装读取（别的盘/便携版）。
- 交付给用户的诊断步骤：① 任务管理器彻底杀 `RimWorldWin64.exe` 后重开，看新会话日志；② 若仍报，取当前日志顶部"Loading mod Rim-Hormones from <path>"行，确认加载的是哪份文件夹；③ 游戏内 Mods 列表确认本 mod 只启用一份。

## 真正根因（22:3x，终结）：defName 写错，不是 XML 结构问题
- 上面两轮"写法标准，无需修改"的结论是**错的**。`Corn` 这个 ThingDef 在原版**根本不存在** —— rimsage search_defs 逐个核实得：
  - 玉米 → `RawCorn`（`Plant_Corn` 是植株）；土豆 → `RawPotatoes`（复数）；鸡蛋 → `EggChickenUnfertilized`/`EggChickenFertilized`（无 `EggChicken`，且属**原版**非 Biotech，上一轮"属 Biotech DLC"的说法亦错）；牛奶 → `Milk`（一致）
  - 炉灶 → `ElectricStove`/`FueledStove`（无 `Stove`）；切石机 → `TableStonecutter`（无 `Stonecutter`）；酿造台 → `Brewery`（一致）
  - SkillDef 只有 12 个，**没有 Stonecutting** → 切石用 `Crafting`（原版 Make_StoneBlocksGranite 即 workSkill=Crafting）
- 已修 4 文件：FromCorn(Corn→RawCorn)、FromPotato(Potato→RawPotatoes)、ProteinExtract(Stove→ElectricStove+FueledStove ×3处；EggChicken→EggChickenUnfertilized+Fertilized)、Salt(Stonecutter→TableStonecutter；Stonecutting→Crafting)。`cp -r Defs` 已同步 Steam 副本并 grep 验证。
- 教训写入 MEMORY.md 铁律：**任何原版 def 引用先 rimsage search_defs 核实，不要凭 label/直觉猜**；label 与 defName 经常不一致。同时把 MEMORY.md 整体去重压缩重写（原文件超限被截断）。

## 修复（23:0x，终结 ArgumentNullException）：statOffsets 错包 `<li>`
- 栈特征：`StatModifier.LoadDataFromXmlCustom` → `ParseHelper.ParseFloat` → `float.Parse(null)` → `ArgumentNullException(Parameter name: s)`；报错文案不提是哪个字段。
- 根因（rimsage 读 Source/RimWorld/StatModifier.cs）：StatModifier 实现 `LoadDataFromXmlCustom`，把 `xmlRoot.Name` 当 stat 名、`xmlRoot.FirstChild.Value` 当数值。写成 `<li><stat>WorkSpeedGlobal</stat><value>-0.20</value></li>` 时 xmlRoot 是 `<li>`、FirstChild 是 `<stat>` **元素节点**（.Value=null）→ 崩。注意：本日志 22:4x 那条把此错归因为"StatDef 名写错(WorkSpeed→WorkSpeedGlobal)"，**归因不完整**——名字改对了但 `<li>` 结构仍在，所以又崩了一次。
- 正确写法：`<statOffsets><WorkSpeedGlobal>-0.20</WorkSpeedGlobal></statOffsets>`（与 statBases/statFactors 同形）。
- 修复 2 文件：`Hediff_MEE_WaterPoisoning.xml`、`Hediff_MEE_Weakness.xml`（同错，只是解析顺序还没轮到）。已 cp 同步 Steam 副本并 grep 验证。
- 对照确认无需改：`capMods` 是 `PawnCapacityModifier`（普通复杂类型、无自定义加载器）→ **必须**保留 `<li><capacity>..</capacity><offset>..</offset></li>`；`vomitMtbDays`(HediffStage.cs:26)、`isBad`/`makesSickThought`/`everCurableByItem`(HediffDef.cs:36/44/64) 均为真实字段。
- 归纳总纲写入 MEMORY.md：**凡实现 `LoadDataFromXmlCustom` 的类型必须简写、禁 `<li>`**，用 `search_source "public void LoadDataFromXmlCustom"` 可一次列全（60+ 处，含 StatModifier/ThingDefCountClass/SkillRequirement/SkillGain/DamageFactor/…）。这条统一解释了今天三次踩坑（products / skillRequirements / statOffsets）。

## 新功能：MEE 四需求中文翻译 + 飘字系统（23:2x）
- 需求：MEE 四个 Need（水分/糖/电解质/蛋白质）中文翻译；修改时头顶飘字；一个总开关 + 四个子开关。
- 实现（主 mod `_indexProj`）：
  - 翻译：`Languages/ChineseSimplified/DefInjected/NeedDef/Hormones.xml` 增 `MEEWater/MEESugar/MEEElectrolytes/MEEProtein` 的 `.label`+`.description`（中文）。NeedDef 本体 label 仍是英文，靠 DefInjected 覆盖。
  - 飘字逻辑：新增 `Source/Logic/MEEMoteLogic.cs`（已加进 Assembly-CSharp.csproj 的 `<Compile Include>`）。`Init()` 订阅 `NeedChangeEvents` 的 4 个事件 → `TryThrow` 用 `MoteMaker.ThrowText`（`-1f` 参数，沿用 AdrenalineLogic 写法）。颜色：水蓝/糖琥珀/电解质青/蛋白粉红。
  - 事件补全：`NeedChangeEvents` 增 `OnProteinChanged` + `FireProteinChanged`（原缺，且 `Need_MEE_Protein` 原来从不发事件）。
  - 触发点统一：`Need_MEE_Base` 原 `OnFallApplied`（仅 NeedInterval 自然衰减触发）改名为 `OnModified`，并在 `Satisfy`/`Consume` 也调用 → 饮水/进食/模块扣减都会触发事件（否则喝糖水不飘字）。4 个子类 override `OnModified` 发各自事件。
  - 防刷屏：飘字阈值 `MoteThreshold=0.02`（占 MaxLevel）；自然衰减每 tick 仅 ~0.0015，被过滤；饮水/进食跳跃 0.1~0.5 才飘。
  - 设置：`RimHormonesMod.Settings` 增 `ShowMEEMotes`(总,默认false) + `ShowMEE{Water,Sugar,Electrolytes,Protein}Motes`(子,默认true)；`DoSettingsWindowContents` 加总开关+4 个缩进子开关；`ExposeData` 加 Scribe；mod 构造器调用 `MEEMoteLogic.Init()`。
- 坑：本沙箱禁 `cmd.exe` 与 `dotnet`，**无法在此 build**。必须用户在本地跑 `copyToRimWorld.bat`（dotnet build + 部署）。且 RimWorld 运行时 DLL 被锁，构建/复制会失败 → 先彻底关游戏再跑 bat。

## 水井/取水机器加账单页签（23:2x）
- 需求：水井(MEE_WaterWell)、取水机器(MEE_WaterExtractor)要像制作台一样有账单清单，可挂「灌装水瓶」(MEE_MakeWaterBottle，产出可饮用的 MEE_WaterBottle)。
- 根因：两建筑 thingClass=Building_WorkTable（有账单栈），但缺 `<inspectorTabs><li>ITab_Bills</li></inspectorTabs>`，故检视面板无账单页签；配方虽用 recipeUsers 指向它们，却无处可挂。
- 修正（对标原版 ElectricStove/Brewery）：Thing_MEE_WaterBuildings.xml 两建筑均补 `<inspectorTabs><li>ITab_Bills</li></inspectorTabs>` + 显式 `<recipes><li>MEE_MakeWaterBottle</li></recipes>` + hasInteractionCell/offset(-1)；配方 MEE_MakeWaterBottle 仍带 MEERecipeMarker（模块关则账单隐藏）。MEE_WaterBottle 本身已有 ingestible(Fluid)+Comp_MEE_Satisfier，确认可饮用。
- XML 校验通过，已 cp 同步 Steam 副本（游戏未运行，纯 XML 可热更）。
- 纠正 MEMORY.md 错误铁律："BuildingProperties 无 billInsertion 字段" 系误判；真机制是 Building_WorkTable 需 inspectorTabs ITab_Bills 才有账单页签，billInsertion 原版不写(默认开)。

## 编译修复：Need_MEE_Protein.cs 缺类闭合 (23:31)
- 用户本地 `dotnet build` 报 CS1513（缺 `}`）：早前把 `OnFallApplied`→`OnModified` 的编辑中，误将类级闭合 `    }` 当命名空间级 `}` 用掉，导致 class 体未关闭。
- 修复：在 `OnModified` 方法 `}` 之后补回类级 `    }`，文件末尾保留命名空间级 `}`。已用脚本确认 4 个子类 + Base 括号全部配平（OK）。
- 教训：编辑带嵌套 `namespace{ class{ method{ } } }` 的 .cs 时，`old_string` 收尾的 `}` 要数清层级；改完跑一次括号配平脚本再交付。

## 编译修复：MEEMoteLogic.cs 误用 DefDatabase.Get (23:32)
- 用户本地 `dotnet build` 报 CS0117：`DefDatabase<NeedDef>` 无 `Get` 方法。
- 修正：`DefDatabase<T>.Get(defName)` 不存在，应改为 `GetNamed(defName)` 或 `GetNamedSilentFail(defName)`（后者找不到静默返回 null，配合 `?.label ?? defName` 兜底）。已用 rimsage 核实 `GetNamedSilentFail` 存在于 1.6。
- 教训：取 Def 用 `DefDatabase<T>.GetNamed[SilentFail](defName)`，莫写 `Get`。

## 23:39 修复：水建筑右键无选项 + MEE 物品放不进存储区
- 问题1（水井/取水机器右键无「账单」选项）：结构已和原版 Brewery/ElectricStove 一致(Building_WorkTable+recipes+ITab_Bills)，但新增 `<building><billInsertion>true</billInsertion>` 显式开启账单。真正主因是配方 `MEE_MakeWaterBottle` 带 `MEERecipeMarker`，**仅 Metabolic Essential 模块启用(IsLoadedMME)时才 AvailableNow**；模块开关未开或未重载则右键空白。已告知用户需在 Mod 设置开启 MEE 模块并重启/重载。
- 问题2（物品放不进存储区）：`MEE_GlucoseMash/WaterBottle/Salt/ProteinExtract` 四个 ResourceBase 物品**全无 `<thingCategories>`**，存储区按类别筛选故无处可放。补类别：GlucoseMash/WaterBottle→`Foods`（葡萄糖原浆按用户要求归食物，水瓶为可饮 Fluid 也归食物）；Salt/ProteinExtract→`Ingredients`。
- 5 个 XML 已 ElementTree 校验 + cp 到 Steam 副本（游戏未运行，纯 XML 热更）。
- 提醒：def 改动需重启/重载存档生效；billInsertion 经验写入 MEMORY.md 铁律（原误判无此字段）。

## 23:53 增强：MEE 模块关闭时水井/取水机器建筑从建造菜单消失
- 用户要求：没开 MEE 模块就不要出现水井这个建筑（比"看得见点不了"干净）。
- 实现（主 mod，无需动模块 DLL）：新增 `MEEBuildingMarker : DefModExtension`（与 MEERecipeMarker 同思路）；`MetaBolicLoadCtrl` 静态构造在 `TryLoad` 后、若 `!IsLoadedMME` 则遍历 `DefDatabase<ThingDef>` 把带标记建筑的 `designationCategory` 置 null → `DesignationCategoryDef.ResolveReferences` 不会将其纳入任何分类，建造菜单即不显示。
- 两个水建筑 XML 加 `<modExtensions><li Class="Hormones.MEEBuildingMarker" /></modExtensions>`。
- 主工程 `dotnet build -c Release` 0 错 0 警；DLL 已 cp 到 Steam Assemblies，建筑 XML 已同步。模块 DLL 未改动无需重编。
- 行为：开 MEE → 水井/取水机器在 Misc 分类可见可用；关 MEE（设置 + 重启）→ 整组建筑与配套配方(MEERecipeMarker)一并隐藏。

## 23:57 修复 XML 报错（用户实测反馈）
- 报错1：`<billInsertion>true</billInsertion>` 不是 BuildingProperties 合法字段 → 从 MEE_WaterWell/WaterExtractor 的 `<building>` 删除（上次误加当保险；账单页签真正机制是 `ITab_Bills`+`<recipes>`，原版也不写此字段）。
- 报错2：`Ingredients` 不是合法 ThingCategoryDef（游戏无此分类）→ 盐/蛋白提取物改归自建 `MEE_Ingredients`（新建 Defs/ThingCategoryDefs/ThingCategory_MEE.xml，parent=ResourcesRaw）。葡萄糖原浆/水瓶的 `Foods` 分类合法无需改。
- 4 个 XML 全部 ElementTree 校验通过 + cp 到 Steam 副本。游戏未运行时纯 XML 热更生效。

## 00:03 修复：水井无法被选中执行装水工作（缺 WorkGiver）
- 报错：Can't find a WorkGiver for a BillGiver MEE_WaterWell / Generating pawn restrictions for a BillGiver without a Workgiver → 殖民者不会去做水井/取水机器的账单。
- 根因：Building_WorkTable(IBillGiver) 必须有一个 WorkGiverDef(giverClass=WorkGiver_DoBill) 经 `fixedBillGiverDefs` 列出该建筑并指定 `workType`，游戏才知道派哪类殖民者执行。水井/取水机器此前未绑定任何 WorkGiverDef。
- 修复：新建 Defs/WorkGiverDefs/WorkGiver_MEE_Water.xml → `DoBillsMEEWater`(workType=Crafting, fixedBillGiverDefs=MEE_WaterWell/MEE_WaterExtractor, requiredCapacities=Manipulation)。已校验 + cp 到 Steam。
- 注：建筑 def 仍受 MEEBuildingMarker 控制(模块关则不出建造菜单)，但 ThingDef 仍在 DefDatabase，fixedBillGiverDefs 跨引用可解析；模块关时配方 AvailableNow=false 自然无账单，workgiver 空转无害。
