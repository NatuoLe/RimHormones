# 2026-08-02 工作日志

## 重写战斗损伤 Hediff 触发逻辑：改为 XML 标签驱动

**需求来源**：用户在 `文档/Hediff/损伤Hediff与触发逻辑.xlsx` → Hediff总览 表新增 5 个关键词列：
【是否可治疗】【战斗时触发】【单位射击触发】【单位近战触发】【单位被真实近战攻击到触发（被击中但不一定造成伤害）】。
要求：只有部分类别才能在战斗触发；概率逻辑不变（仍走原公式），标签只是"大前提"。

**Excel 标记实况（10 个损伤）**：
- CombatJointStrain / FallJointStrain：可治疗=是，战斗触发=是，onMelee=是，onMeleeHitTaken=是
- VisualStrain / AuditoryStrain：可治疗=否，战斗触发=是，onShoot=是
- LaborMuscleStrain / DiggingMuscleStrain：可治疗=是，无战斗标签（纯劳作路径）
- CardioOverexert / SuffocationStrain / CombatEnduranceExhaust / MetabolicExhaust：可治疗=否，无战斗标签

**实现（三层）**：
1. **新扩展** `_indexProj/Source/Logic/PhysiqueLogic/StrainHediffTags.cs`（已登记 csproj）：
   - `StrainTriggerSource` 枚举（Shoot / Melee / MeleeHitTaken）
   - `StrainHediffExt : DefModExtension`（combatTriggered 总开关 + onShoot/onMelee/onMeleeHitTaken）+ `RespondsTo(source)`
   - `StrainHediffTags.GetPool(source)`：遍历 DefDatabase 按标签建池，**按触发源缓存**
   - 注：可治疗性走原版 `HediffDef.tendable`，不进扩展
2. **XML** `Defs/HediffDefs/Hediff_StrainPool.xml`：10 个损伤按 Excel 写入 `<modExtensions><li Class="Hormones.StrainHediffExt">` + 修正 tendable（Labor/Digging/CombatJoint/FallJoint 改 true）。
   脚本 `Dev/tag_strain_hediffs.py`（幂等，可重跑；改 Excel 后同步改 SPEC 再跑）。`PhysiqueStrainExhausted` 不在 SPEC 内，正确跳过。
3. **C#** `AdrenalineLogic.cs`：
   - `ApplyRandomOverexertHediff(pawn, level, source)` 删掉硬编码 3 个 defName 列表，改调 `StrainHediffTags.GetPool(source)`
   - 新增 `PickByPhysiqueTier(pool, level)`：档位比例沿用历史（<5: 重.30/中.60；5-7: 重.15/中.50；≥8: 重.05/中.30），**档位空则向更轻档回退**保证抽得到
   - 档位归类改为静态 HashSet `ModerateTierDefNames`(含新入池的 Visual/Auditory) / `SevereTierDefNames`；飘字分色复用
   - **二进制兼容**：`TryApplyOverexertDamage(Pawn, float)` 旧签名保留为垫片转发（默认 Melee 源），新增三参重载
4. **触发点** `HormonesLogic.cs`：
   - 近战 patch 传 `StrainTriggerSource.Melee`；射击 patch 传 `Shoot`
   - **新增受击方判定**：同一个 `Verb_MeleeAttack.TryCastShot` postfix 内，用 `bool __result` 判定"真实命中"（true = 未 miss 且未闪避，**护甲格挡 deflected 仍为 true**，恰合需求），`__instance.CurrentTarget.Pawn` 取受击者，走 `MeleeHitTaken` 源

**关键源码结论（rimsage 核实）**：`Verb_MeleeAttack.TryCastShot` 返回值语义 = 真实命中；`Verb.CurrentTarget` 是 public；受击方判定无需额外 patch DamageWorker。

**当前池效果**：射击→{VisualStrain, AuditoryStrain}（均中度档）；近战/被近战命中→{CombatJointStrain(轻), FallJointStrain(重)}。
CardioOverexert/SuffocationStrain/CombatEnduranceExhaust/MetabolicExhaust 已**退出战斗池**（按 Excel），仅保留其他路径。

**验证**：XML ElementTree 解析通过（11 个 HediffDef）；编译 0 错 0 警；grep 确认 4 个新类型入 DLL；DLL(MD5 98fee1aa…)+XML(886a8436…) 已 cp Steam。CE 包无需重编译（旧签名垫片保留）。

## 追加：档位与比例也全部 XML 配置化（用户要求"最好通过 xml 配置"）

**动机**：上一步档位归类还留在 C# 的 HashSet（ModerateTierDefNames/SevereTierDefNames），比例也是 if/else 硬编码。现已彻底外移到 XML。

**新增**：
1. `StrainHediffExt.severityTier`（枚举 `StrainSeverityTier`: Mild/Moderate/Severe）——每个损伤在自己的 modExtensions 里声明档位。已给 10 个损伤写入（脚本 SPEC 加第 6 列）。
   档位分配：Mild=Labor/Digging/CombatJoint；Moderate=Cardio/Suffocation/Visual/Auditory；Severe=FallJoint/CombatEndurance/Metabolic。
2. **新 Def 类型** `StrainTierRulesDef : Def` + `StrainTierRule`（maxPhysiqueLevel 上界不含 / severeChance / moderateChance），配置文件 `Defs/MiscDefs/StrainTierRules.xml`，defName `Hormones_StrainTierRules`。
   `RuleFor(level)` 取"命中且 maxPhysiqueLevel 最小"的一条（升序匹配语义）。
3. `StrainHediffTags.GetTier(def)` / `.TierRules`（带缓存 + 缺失时 Log.Warning）。
4. `AdrenalineLogic`：删掉两个 HashSet；`PickByPhysiqueTier` 改读 XML 档位与比例，XML 缺失时回退内置默认值（0.30/0.60、0.15/0.50、0.05/0.30）；飘字颜色也改用 `GetTier`。

**自定义 Def 类型的 XML 写法备忘**：根元素直接用**完全限定类名** `<Hormones.StrainTierRulesDef>`（不是 `<Def Class="...">`）；HediffDef 内的扩展仍用 `<li Class="Hormones.StrainHediffExt">`。

**现在改平衡完全不用编译**：调档位改 Hediff_StrainPool.xml 的 severityTier；调比例改 StrainTierRules.xml；调池子改三个 on* 布尔。

**验证**：两个 XML ElementTree 通过；编译 0 错 0 警；grep 确认 StrainTierRulesDef/StrainSeverityTier/severityTier 入 DLL；DLL(MD5 a30da860…)+2 XML 已 cp Steam（Defs/MiscDefs 为新建目录）。CE 包无需动。

## 新功能：肾上腺素长期堆积损伤（2026-08-02）

**需求**：Excel 新增列【肾上腺素长期堆积造成的Hediff】，标记了 CardioOverexert / SuffocationStrain / CombatEnduranceExhaust / MetabolicExhaust 四项。要求"处于什么阶段就触发什么阶段的 Hediff"，每 10 秒检测一次，体魄 0≈10% / 体魄 12≈3%。

**公式确认过程（用户拍板 3 项）**：
- 提醒用户 10 秒一次 = 一小时 250 次检测，10% 会导致 1 分钟内 47% 必中 → 用户选**缩到千分级**（原值 ÷10）。
- 阶段映射：用户选 **Low→轻 / Medium→中 / High→重**。
- 体魄 ≥13：用户选**同样豁免**（复用 IsAdrenalineExempt）。
- Mild 档缺口（四个标记项无 Mild）：用户选**方案 1 = Low 阶段不触发**（enabled=false）。

**最终公式**：`chance = (0.010 − 0.000583 × 体魄) × 阶段chanceFactor × 玩家总倍率`，每 600 tick 检测。
体魄 0 → 1.00%/次（期望约 16 分钟一次）；体魄 12 → 0.30%/次（约 55 分钟）；≥13 → 0。

**实现**：
1. `PhysiqueDefine.cs`：新增 `AdrenalineBuildupCheckIntervalTicks=600` / `BaseChance=0.010` / `ChancePerPhysique=0.000583` / `DefaultAdrenalineBuildupGlobalMult=1`。
2. `StrainHediffTags.cs`：
   - `StrainTriggerSource` 加 `AdrenalineBuildup`；`StrainHediffExt` 加 `onAdrenalineBuildup`。
   - **重要语义**：`RespondsTo` 里长期堆积**不受 combatTriggered 总开关约束**（它不是战斗触发），单独由 onAdrenalineBuildup 控制；四个标记项 combatTriggered 均为 false 仍能进池。
   - 新 Def 类型 `StrainAdrenalineStageRulesDef` + `StrainAdrenalineStageRule`（level / enabled / tier / chanceFactor），`RuleFor(level)` 精确匹配；`StageRules` 属性带缓存与缺失告警。
   - BuildCache 加入新触发源。
3. `Defs/MiscDefs/StrainAdrenalineStageRules.xml`：defName `Hormones_StrainAdrenalineStageRules`，Low(enabled=false) / Medium→Moderate / High→Severe。
4. `AdrenalineLogic.cs`：新增 public `TryApplyAdrenalineBuildupDamage(Pawn)`；**重构出 `ApplyOverexertHediff(pawn, hediffDef)`** 供战斗透支与长期堆积共用落地流程（部件映射/义体跳过/按部件判重/飘字）。长期堆积**严格按阶段档位抽取，档位空则不触发（不回退）**，与战斗透支的回退策略不同。
5. `HormonesComponent.CompTick`：`IsHashIntervalTick(600)` 调用。
6. Settings：`AdrenalineBuildupGlobalMult` 滑块（0~3，0=关）+ 持久化。
7. `Dev/tag_strain_hediffs.py` SPEC 加第 7 列，四项打 `onAdrenalineBuildup=true`。

**验证**：三个 XML ElementTree 通过；编译 0 错 0 警；grep 确认 4 个新符号入 DLL；DLL(MD5 730d021a…)+2 XML 已 cp Steam。CE 包无需动（只新增 public 方法，未改旧签名）。

## 同步用户在 xlsx 的第二批改动（文本/数值/禁用阶段/概率÷4）

**方法**：git show HEAD 旧 xlsx ↔ 现 xlsx 逐单元格 diff（openpyxl），精确定位全部改动。editor_sdk(39099) 未运行 → 直接用 openpyxl 读写 xlsx（读 data_only=True，写普通 load，纯表格无公式/图表，安全）。

**1. XML（Hediff_StrainPool.xml）文本+数值**：
- SuffocationStrain 中文名 缺氧损伤→**过度呼吸**（label 与第一阶段同名）
- 阶段改名：Cardio 心悸→过度兴奋/心律失常与过度换气→心律失常；Suffocation 肺部灼热→过度呼吸/呼吸困难→碱中毒；FallJoint 中→膝关节扭伤/重→腿部韧带撕裂；CombatEndurance 中 神经抑制→神经耗竭；Visual 视觉疲劳→畏光/眼肌痉挛→光灼伤；Auditory 中 听觉过敏→听力损伤
- 数值：Cardio 中 BloodPumping/Breathing 0.85→**1.15**；Suffocation 中 Breathing 0.75→0.8、BloodPumping 0.85→0.9；CombatEndurance 轻 pain -0.05→-0.1、Consciousness 1.05→**1.2**，中 Consciousness 0.85→0.9；Visual 轻 pain 0.02→删、Sight 0.90→0.95，中 Sight 0.75→0.9
- 6 个标「重新写」的 description 已重写（Cardio/Suffocation/CombatJoint/FallJoint/Visual/Auditory）

**2. 禁用 4 个重度阶段**（xlsx 标「无法触发」）：CardioOverexert 心肌缺氧 / SuffocationStrain 肺水肿 / CombatEnduranceExhaust 神经耗竭(重) / AuditoryStrain 听力损伤(重)。做法 = **注释掉第三阶段 li + maxSeverity 1.0→0.74**（双保险，注释内写明恢复方法和新数值）。
- ⚠️ 遗留：CardioOverexert 重度 BloodPumping 在 xlsx 里值残缺（"×0."），已在 XML 注释标注待用户补。

**3. 战斗透支概率 ÷4 补齐**：0.4.4 只除了 base（0.2→0.05），本次 `Define.AdrenalineOverexertChancePerPhysique` 0.04→**0.01**，公式变为 (0.05+0.01×(13−体魄))×倍率，恰为原公式整体 ÷4。CE 包不引用该常量（已 grep），无需重编译。
- 射击倍率维持 0.1（0.4.4 从 0.5 改的，非本次范围）→ xlsx 表内"×0.5"备注已全部改 ×0.1。（**已过时**：后改为 0.25，见文末「射击倍率定为 0.25」）

**4. xlsx 回写**：触发逻辑表公式行、表A（L0 近战 0.72→0.18 等 7 行）、表C（7 行×3 列+备注）、第 42 行补充说明公式，全部更新为 ÷4 后数值。

**验证**：XML ElementTree 通过（11 def，4 个禁用 def maxSeverity=0.74 且只剩 2 阶段）；编译 0 错 0 警；XML+DLL 已 cp Steam。

## xlsx 第三批改动：2 个重度阶段重新启用

- **CardioOverexert 重**：心肌缺氧→**心肌缺血**，标注改「允许触发」→ 取消注释、maxSeverity 回 1.0；BloodPumping 残缺值用户确认为 **×0.80**（其余 Breathing ×0.88 / Consciousness ×0.85 / MoveSpeed ×0.75）；description 同步改心肌缺血。
- **SuffocationStrain 重**：肺水肿→**呼吸衰竭**，同样重新启用（Breathing ×0.60 / BloodPumping ×0.8 / Consciousness ×0.80）；description 同步。
- CombatEnduranceExhaust 重、AuditoryStrain 重仍为「无法触发」，保持注释+0.74 封顶不变。
- **总览新列「肾上腺素长期堆积造成的Hediff」**（Cardio/Suffocation/CombatEndurance/Metabolic 标「是」）：**用户说另一个 agent 处理**，本次不动。注意这 4 个 hediff 目前在游戏里无任何触发路径（已退出战斗池），代码里也无"肾上腺素积压"机制。（后注：另一 agent 已实现该机制，见上文 70-83 行）
- 纯 XML 改动，未编译；XML 已 cp Steam。

## xlsx 第四批改动：全部损伤不可治疗 + 阶段文本/数值

- **tendable 全否**：Labor/Digging/CombatJoint/FallJoint 4 个 true→false（其余本就 false）。至此 10 个损伤全部 tendable=false。
- **阶段改名**：Digging 肩部酸痛→酸痛/肩袖损伤→拉伤/肩关节脱位→关节脱位；CombatJoint 关节扭伤→扭伤(轻)/韧带损伤→扭伤(中)；FallJoint 踝关节扭伤→关节扭伤(轻)/膝关节扭伤→关节扭伤(中)。对应 description 同步精简。
- **删 statFactors**：CardioOverexert 中(MoveSpeed 0.90)与重(0.75)、SuffocationStrain 中(0.92) 的 statFactors 块按 xlsx 空格删除。
- **FallJointStrain 数值**：轻 Moving 0.90→0.95；中 Moving 0.75→0.85 且删 Manipulation ×0.85；重 Moving 0.60→0.85。
- **MetabolicExhaust 数值**：轻 Metabolism 1.15→0.9/BloodFiltration 1.05→0.95；中 0.80→0.85/0.90→0.95；重 0.60→0.80/0.70→0.85/BloodPumping 0.80→0.85。轻度从"代偿性增强(>1)"改为全面抑制(<1)。
- 注意：另一 agent 加的 `onAdrenalineBuildup` 标签已保留未动。
- XML ElementTree 通过（11 def）；纯 XML 改动未编译；已 cp Steam。

## 排查：射击无法触发损伤 → 射击门槛 High→Low

**用户反馈**：射击好像无法触发损伤 hediff。
**诊断**（非 bug，机制叠加）：
1. `AdrenalineRangedAttackBase=0f` → 射击完全不产肾上腺素（设计如此）；
2. 肾上腺素唯一增长源 = 15 格内敌对战斗单位插值（0.03×(1.8−0.04L)/s），后排射手吃不到且净衰减；
3. 体魄 ≥8 时即使贴脸净增长 ≈0 或为负（0.03×(1.8−0.04L) − (0.02+0.003L)）；
4. 触发门槛要求 effects.Level ≥ High（severity≥0.75）→ 射手实战永远达不到。
补丁/Shoot 池标签/判重均正常。

**修复（用户选定：门槛降到 Low）**：
- `HormonesLogic.cs` Verb_LaunchProjectile patch：`effects.Level >= AdrenalineLevel.High` → `>= AdrenalineLevel.Low`（仅射击；近战/被近战仍 High）。
- `AdrenalineLogic.cs` TryApplyOverexertDamage 内侧兜底门槛按源区分：Shoot → AdrenalineThresholdDormant(0.15)，其余 → AdrenalineThresholdLow(0.5)。
- 未动 public 签名，CE 包无需重编译。编译 0 错 0 警。
- **残余限制**：射击仍不产肾上腺素，射手需靠挨打(+0.10×系数/次)或贴近敌人叠到 0.15 才能开始掷骰；每发概率 0.6%~1.8%。

**⚠️ 未完成部署**：RimWorld 运行中(PID 36592) DLL 锁定、xlsx 被 Excel 占用，两处 cp/写入均 Permission denied。待用户关闭后补：① DLL cp Steam；② xlsx 触发逻辑 R22/R42 门槛说明更新（脚本已就绪，值见对话）。（后注：用户选"稍后自己处理"；DLL 已在当天 12:55 随「流血产肾上腺素」的编译一起部署完成。xlsx R22/R42 备注仍未写入。）

## 新增：流血产肾上腺素（两个分档生成逻辑）

**需求**（用户表格）：`AdrenalineBloodingLowBase=0.035`（流血<150% 每秒生成）、`AdrenalineBloodingHighBase=0.065`（流血>150% 每秒生成）。

**实现**：
- `Define.cs`：`AdrenalineBloodingLowBase=0.035f` / `AdrenalineBloodingHighBase=0.065f` / `AdrenalineBloodingThreshold=1.5f`（= HediffSet.BleedRateTotal 1.5，即 150%）。
- `AdrenalineProducer.cs`：
  - 新增 `GetBleedingAdrenalineGain(pawn, physique)`：BleedRateTotal ≤0→0；<1.5 低档、>1.5 高档，再乘 `CalculateGenerationMultiplier(physique)`（与其他 *Base 常量一致的体魄系数惯例）。
  - `CalculateNetChangePerSecond` 加入 bleedingGain（每 60tick≈1s 结算）。
  - **关键补洞**：`ProcessAdrenalineDynamic` 原来 adrenaline==null 直接 return → 旧伤流血时 hediff 已衰减移除、流血生成永不生效。现改为：无 hediff 但流血增益>0 时**现场创建** Adrenaline hediff 并写入首秒增益。
- 不抛飘字（与战斗插值一致，避免每秒刷屏）。
- 注意：Define.cs 里 `AdrenalineRangedOverexertChanceMultiplier` 已被改成 0.25（非我所改，保留未动）。
- 编译 0 错 0 警；grep 确认 GetBleedingAdrenalineGain/AdrenalineBloodingLowBase 入 DLL；DLL 已 cp Steam（游戏已关）。未动 public 签名，CE 包无需重编译。

## 射击倍率定为 0.25（用户决策：保留代码、更新 Excel）

- `AdrenalineRangedOverexertChanceMultiplier` 现状 **0.25** 为用户确认的最终值（历史：0.5 → 0.4.4 改 0.1 → 后改 0.25）。
- xlsx 触发逻辑已全面同步 ×0.25：R21 备注、R23 关键参数、R42 补充说明、表A 射击列（0.045/0.0375/0.0325/0.0275/0.025/0.02/0.015）、表C 备注。
- 同时补写了之前欠的 R22/R42「射击门槛 Low(≥0.15)」备注。

## 射击仍不触发 root cause：CE 补丁包漏调 TryApplyOverexertDamage

**机制**：CE 的 `Verb_LaunchProjectileCE` 直接继承 Verse.Verb（非原版 Verb_LaunchProjectile），本体 shooting postfix 永不触发，射击判定全靠 CE 补丁包。而 `Patch_CombatExtended_Verbs.cs` 的远程 postfix **只调了 OnAttack，从未调 TryApplyOverexertDamage**（历史遗留：当时只做"恢复肾上腺素入口"）→ CE 下射击永远不出损伤。近战 CE postfix 也缺 MeleeHitTaken 受击方判定。

**修复（D:\RimMods\RimHormonesCE\Source\RimHormonesCE\Patch_CombatExtended_Verbs.cs）**：
- CE 远程 postfix：补 `TryApplyOverexertDamage(attacker, Define.AdrenalineRangedOverexertChanceMultiplier, StrainTriggerSource.Shoot)`，门槛 Low（与本体一致）；注释标注常量为编译期内联、本体改值需重编译。
- CE 近战 postfix：加 `bool __result` 参数 + MeleeHitTaken 受击方分支（对齐本体逻辑）。
- 流程遵守铁律：先把新 RimHormones.dll cp 到 `RimHormonesCE\Refs\` 再 `dotnet build -c Release`（0 错 0 警），产物 `Source\bin\RimHormonesCE.dll` 已 cp 到 `Steam\...\Mods\RimHormonesCE\Assemblies\`（grep 确认 StrainTriggerSource 入 DLL）。

## DrinkingwaterIsGood（饮品 mod）报错修复包

项目：`D:\RimMods\Rim-Hormones\DrinkingwaterIsGood\`（packageId Lenatuo.functiondrinksexpanded，依赖 DBH Lite + VCE + Rim-Hormones）。部署目标 `Steam\...\Mods\DrinkingwaterIsGood\`。

1. **socialPropernessMatters 报错×2（同一根因）**：1.6 该字段已从 `IngestibleProperties` 移到 **`ThingDef` 根级**（Verse/ThingDef.cs:282）。写在 `<ingestible>` 里报"doesn't correspond to any field"，且根级缺省 false 触发 warden 送饭 Config error。修法 = 从 ingestible 删掉、加到 DWG_DrinkBase 根级，**一处修复两个报错**。
2. **SoundDef Recipe_BrewBeer 不存在**：1.6 改名 **`Recipe_Brewing`**（另有 Brewing_Start/Stop），4 处 soundWorking 已替换。
3. **PatchMain 静态构造崩溃**：DrinkPatch 用反射找 `Pawn.Ingest`——**1.6 不存在此方法**，GetMethod 返回 null → PatchAll 抛 HarmonyException → 整个 mod 失效。改 patch **`Thing.Ingested(Pawn ingester, float nutritionWanted)`**（所有进食路径最终汇聚点），类改名 Thing_Ingested_Patch，逻辑不变。编译 0 错 0 警。
4. **MakeElectrolyteDrink 重复**：Steam 部署目录残留旧版 3 文件（RecipeDefs/ThingDefs/ThoughtDefs_ElectrolyteDrink.xml），与新版 *_Drinks.xml 重复。已删（旧文件里的 MakeElectrolyteDrinkVCE 精制配方也被新设计合并掉，如需恢复单独说）。
5. **隐藏饮品 Hediff**：HediffDef 无 visible 字段；可见性走 `Hediff.Visible => visible || CurStage.becomeVisible` → 4 个饮品 Buff 的 stage 加 **`<becomeVisible>false</becomeVisible>`** 即从健康面板隐藏。

**⚠️ 待办**：DrinkingwaterIsGood.dll 部署时游戏在运行被锁，用户选"稍后自己处理"——需 cp `Source\bin\DrinkingwaterIsGood.dll` → `Steam\...\Mods\DrinkingwaterIsGood\Assemblies\`（XML 已全部部署）。
