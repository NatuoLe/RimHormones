# 2026-08-09 工作日志

## 劳损速率外置化（饮品拓展解耦，已完成代码+编译，待部署）
- 用户要求：饮品拓展（Function Drinks Expanded）对象不应写在本体；改"劳损速率"时，在 Need_strain 加字段 `ExtraModify_StrainEffect`，由外置 Mod 运行时修改该变量生效，而非本体硬编码饮品 defName。
- `Need_MuscleStrain.cs`：
  - 新增 `public float ExtraModify_StrainEffect = 1.0f;`（劳损积累速率额外乘区，瞬态不持久化）。
  - `AddStrain(float amount)` 移除对 `DrinkElectrolyte/DrinkEnergyDrink/DrinkFruitJuice` 的硬编码降速（原 0.7/0.75），改为 `amount *= ExtraModify_StrainEffect;`（所有劳损积累唯一汇点：工作 PhysiquePatches:197 + 锻炼 ExerciseWork:100）。
- `PhysiqueLgc.cs`：`GetMuscleStrainConsumeMultiplier()` 移除饮品硬编码块（原 stageMult*=0.75），仅留体魄阶段倍率；饮品效果统一收敛到 `ExtraModify_StrainEffect`。顺带修掉原"consume 倍率 + AddStrain"双重叠加。
- 编译 0 错 0 警；**部署被阻**：RimWorld 正在运行（RimWorldWin64.exe PID 39920）占用 Steam 侧 DLL，cp 报 Permission denied。需用户完全关闭游戏后我再 cp 部署。
- 已写入项目 MEMORY.md 新增"本体/外置 Mod 解耦铁律"，并修正失眠快照行（5h/12500t、纯 Hediff、InsomniaMood -1）。

## 待用户定：其余饮品硬编码（非劳损速率）是否也外置
- 本体里**非劳损速率**的饮品硬编码仍有 3 处（按 defName 查饮品 hediff，属 Function Drinks 内容）：
  - `HormonesLogic.cs` ~328：`DrinkFruitJuice` → 体魄经验 +25%。
  - `HormonesComponent.cs` ~236：`DrinkEnergyDrink` → 视为"当成一次锻炼"。
  - `Need_Cortisol.cs` ~188：`DrinkMilkTea` → 改皮质醇相关效果。
- 用户本次只指定了劳损速率的 `ExtraModify_StrainEffect` 机制，未给 XP/锻炼/皮质醇的外置字段方案。需确认是否同样外置（各自加字段）或暂留。

## 封装方法 + 其余三处饮品硬编码全部外置（已完成，已部署）
- 用户要求：不暴露 public 字段让外置 Mod 直改，而是**封装方法**给外部 Mod 调用；且其余三处饮品硬编码也一并外置。
- 废除旧 `ExtraModify_StrainEffect` 公开字段，改为 `Need_MuscleStrain` 私有字段 + `SetExtraStrainRateMultiplier/Reset/Get` 方法（劳损速率）。
- 新增另外三处外置 API（均 private 字段 + Set/Reset/Get 方法，本体零饮品 defName 硬编码）：
  - 体魄经验+25% → `Need_MuscleStrain.SetExtraPhysiqueXpMultiplier/Reset/Get`（`HormonesLogic.SkillRecord_Learn_Physique_Patch` 跨类 `TryGetNeed<Need_MuscleStrain>` 读 Get）。
  - 当成锻炼（抑制每日体魄衰减）→ `HormonesComponent.SetDailyDecaySuppressed(bool)/Reset`。
  - 改皮质醇（额外衰减13%/日≈ +3.25/interval）→ `Need_Cortisol.SetExtraCortisolDecay(float)/Reset`。
- 本体四处饮品硬编码（DrinkElectrolyte/DrinkEnergyDrink/DrinkFruitJuice/DrinkMilkTea）现已**全部移除**，grep 无残留。
- 编译 0 错 0 警；RimWorld 已关闭，**DLL 已部署到 Steam**（97792B）；CE Refs 已更新并**重编 CE 补丁包**（0 错 0 警，二进制兼容已确认，无需重新发布 CE 产物）。
- 外置 Mod（Function Drinks）需在饮品 hediff 生效时调上述方法、失效时调 Reset；尚未改 Function-Drinks-Expanded 仓库源码（需用户授权）。

## Function-Drinks-Expanded 适配外置 API（已完成并部署）
- 任务：让饮品 mod 通过主 mod 暴露的 Set/Reset 方法施加四类效果，而非主 mod 硬编码检测饮品 defName。
- 新增 `Source/DrinkingwaterIsGood/HediffComp_DrinkEffect.cs`：
  - `HediffCompProperties_DrinkEffect`（XML 配置，字段：`strainRateMultiplier`/`physiqueXpMultiplier`/`suppressDailyDecay`/`corticalDecayPerInterval`→实为 `cortisolDecayPerInterval`）。
  - `HediffComp_DrinkEffect`：采用**聚合重算**策略（多款饮品可共存），每次 add/remove/load 扫描本 pawn 全部饮品 Hediff，合并后一次性写入主 mod。
    - 触发点：`CompPostPostAdd`(饮用添加) / `CompPostPostRemoved`(移除/过期) / `CompExposeData`(重载存档恢复，1.6 无 CompPostLoad) / `CompPostTick` 首 tick 兜底。
    - 合并规则：劳损取各饮品最小乘区、体魄经验取最大、抑制衰减取 OR、皮质醇衰减取求和。
- 4 款饮品 XML 各加 `<li Class="DrinkingwaterIsGood.HediffCompProperties_DrinkEffect">`：
  - Electrolyte：`strainRateMultiplier=0.75`（劳损-25%）
  - EnergyDrink：`strainRateMultiplier=0.75` + `suppressDailyDecay=true`（劳损-25% + 当成锻炼）
  - FruitJuice：`physiqueXpMultiplier=1.25`（体魄经验+25%）
  - MilkTea：`cortisolDecayPerInterval=3.25`（皮质醇额外衰减≈13%/日）
- `Refs/` 新增 `RimHormones.dll`（编译期引用，97792B）；csproj 登记新 .cs + RimHormones 引用。
- 编译 0 错 0 警；构建产物 `DrinkingwaterIsGood.dll`(14848B) 暂存包内 Assemblies 并**已 cp 到 Steam `Mods/DrinkingwaterIsGood`**（Defs/Languages/About 同步）。RimWorld 仍运行（PID 13208），内存为旧 DLL，**完全重启游戏**后生效。
- 注意：此前咖啡/茶等其它饮品 defName（如 `ElectrolyteDrink`→`DrinkElectrolyte`）映射由 `DrinkPatch.cs` 的 `AddDrinkHediff` 负责，本次未改动其映射逻辑，仅把"效果施加"从主 mod 迁到饮品 mod 的 HediffComp。

## 皮质醇机制图生成（已完成）
- 用户要求：根据「皮质醇数值.xlsx」生成一张图片，展示皮质醇被哪些事件影响、触发哪些坏 Hediff。
- 产出：`文档/Hediff/皮质醇机制图.png`（209KB 光栅）+ `.svg`（33KB 矢量源文件），含四区块：①三档位阈值与效果、②增长/衰减来源双栏列表、③体魄修正表、④坏Hediff三卡片（神经衰弱/快感缺失/失眠）。
- 技术方案：Python openpyxl 读 Excel → Python 字符串拼接 SVG → Edge headless 截图转 PNG。生成脚本 `_gen_img.py` 保留在 Hediff 目录。
- ⚠️ 数据差异：Excel 中失眠=5000tick(≈2h)，但代码已改为12500tick(5h)。图片按 Excel 原值绘制，待用户确认是否同步更新。
- **边缘体魄功能图 HTML**（文档/Hediff/）：从简明 PNG→科技风→RimWorld Widget 风格迭代，最终版含：环世界官方 Logo 中心枢纽、三系统大面板（体魄/皮质醇/肾上腺素）、向外扩散子标签卡片（连线）、5 张 wiki 生物精灵水印填充空白。全部图片来自 rimworldwiki.com，离线自包含在 rw_assets/。同时生成了皮质醇机制图（中英双语 HTML）。

## 皮质醇机制图 RimWorld 风格 HTML（已完成）
- 用户要求：去掉详细数值、言简意赅；后迭代为 RimWorld 风格 HTML（深色 Widget 面板+金色标题）。
- 产出：`文档/Hediff/皮质醇机制图.html`（中文版）+ `_en.html`（英文版），自包含无外部依赖。
- 内容：左栏「皮质醇 ↑」10 项抬升来源（每行3胶囊排列）、右栏「皮质醇 ↓」5 项降低来源（同款胶囊）、中央节点→三坏Hediff卡片（神经衰弱/快感缺失/失眠）。底部版本号取自 About.xml = beta 0.4.7。

## beta 0.47 开发进度日志整理（已完成，写入 Obsidian）
- 用户要求：总结本次皮质醇相关更新，写入 `F:/ObsdianProj/.../Rim-Hormones/开发进度日志/边缘体魄&激素 beta0.47.md`。
- 经 git diff + 代码核实，本次改动三大块：① 皮质醇整体速率×2（前期更易累积）、② 神经衰弱覆盖效应`StrainCoverEffectMultiplier=0.67`（拖慢劳损/皮质醇恢复，仅下降方向）经`PhysiqueLgc.GetStrainCoverEffect`接入两 Need、③ 重写社交补丁（Slight/Insult 权重 ×0.5/2/4 + NegativeInteractionChanceFactor ×0.5/2/4，按档位，"戾气重难听人说话"）。另补全用户草稿未写完的"神经衰弱导致…"/"皮质醇过高导致…"两节（失眠前置、坏症状组加权抽取、快感缺失等）。
- 原始草稿文件为未完成片段，已整体重写为结构化开发日志。

## 边缘体魄功能关联图（已完成）
- 用户要求：根据 `体魄系统数值一览.xlsx` + `皮质醇数值.xlsx` + `肾上腺素数值.xlsx` 三表生成功能介绍图，无数值、简约、中心点辐射关联图。后迭代为科技风 HTML → 最终定为 **RimWorld UI 风格**。
- 产出：`文档/Hediff/边缘体魄功能图.html`（6KB 自包含 HTML）+ `_tech.png`（302KB PNG 快照）。
- 视觉风格：深色石板背景(#1b1b1b) + 金色渐变圆形徽章中心节点（Georgia 衬线"边缘体魄 / RIMPHYSIQUE"）+ 三张 Widget 面板（斜面3D边框 + 金色标题栏），配色绿(体魄)/橙(皮质醇)/红(肾上腺素)，连线暗金色实线静态。底部版本号 beta 0.4.7。

## 可选模块 MetabolicEssential（运行时开关加载，路线A，已完成并部署）
- 用户需求：新建二级 DLL `MetabolicEssential.dll`，游戏内开关控制是否加载；**运行期改开关须重启客户端**（加载只在启动期一次）；架构要利于后期拆成独立 mod。选路线A（同 mod 内 + Assembly.LoadFrom 运行时加载，非独立 mod）。
- 主 mod 新增（`_indexProj/Source/Metabolic/`）：`IMetabolicModule.cs`（接口，主 mod 只认此、不编译引用实现）、`MetabolicLoader.cs`（TryLoad：读 Settings 开关，从 `<mod根>/MetabolicEssential/MetabolicEssential.dll` Assembly.LoadFrom，反射找实现类调 Init，置 IsLoaded）、`MetabolicBootstrap.cs`（[StaticConstructorOnStartup] 全部 mod 加载后触发 TryLoad）。
- Settings：`RimHormonesMod.cs` 加 `EnableMetabolicEssential`(默认 false)+Scribe；设置页复选框 + `if(EnableMetabolicEssential != MetabolicLoader.IsLoaded)` 提示需重启。三新 .cs 已加进 `Assembly-CSharp.csproj` Compile 列表。
- 二级工程：`_metabolicEssentialsExtended/MetabolicEssential.csproj` + `Source/MetabolicEssentialModule.cs`（实现 IMetabolicModule，Init 内 `new Harmony("Lenatuo.metabolicEssential").PatchAll(...)`）。net48，引用 `_indexProj` 下 0Harmony/Assembly-CSharp/UnityEngine*/RimHormones.dll，全部 `Private=false`；产物仅 MetabolicEssential.dll+pdb。
- **关键铁律：二级 DLL 必须放 mod 根 `MetabolicEssential/` 子目录（RimWorld 不自动加载），绝不进 Assemblies/**；否则开关失效。
- 编译：主 mod 0 错 0 警（100352B）；二级 0 错 0 警（4608B）。已部署到项目根 `MetabolicEssential/` + `Assemblies/RimHormones.dll`，以及 Steam mod 同路径副本。
- 后期拆独立 mod：把 `_metabolicEssentialsExtended` 移到独立目录+About.xml，启用 `MetabolicEssentialModule.cs` 注释掉的 [StaticConstructorOnStartup] Bootstrap（自带 Mod 子类），接口契约不变即可平滑迁移。

## copyToRimWorld.bat 重写（支持双 DLL，已完成）
- 起因：用户要求脚本把新增的 MetabolicEssential.dll 也打进 mod 文件夹。检查发现**原脚本本身已失效**：它按"脚本位于 `_indexProj/` 内"写死路径（`%PROJECT_DIR%Assembly-CSharp.csproj`、`%PROJECT_DIR%..\About`），而文件实际在 mod 根，跑起来会直接报 csproj not found。
- 重写为 6 步：①build `_indexProj\Assembly-CSharp.csproj` ②build `_metabolicEssentialsExtended\MetabolicEssential.csproj`（**必须在①之后**，它引用 `_indexProj\bin\Release\net48\RimHormones.dll`）③同步 DLL 到项目根 `Assemblies\` + `MetabolicEssential\` ④清理 dest 旧文件 ⑤xcopy About/Defs/Patches/Languages/**Config**（Config 为本次新增）⑥拷 DLL 到 Steam 侧 `Assemblies\` 与 `MetabolicEssential\`。
- 内置双重保险：项目根与 Steam 侧 `Assemblies\MetabolicEssential.dll` 若存在一律 del（防止误入 Assemblies 导致 RimWorld 自动加载、开关失效）。二级工程缺失时用 `META_BUILT=0` 跳过而非报错。
- 两工程 dotnet build 均 0 错 0 警验证通过；脚本内所有引用路径已逐条 exists 校验。注：沙箱禁止我直接调 cmd 跑 .bat，脚本需用户双击自测。

## MetabolicEssential 改为输出到 Assemblies/（架构调整，已完成并部署）
- 用户指令：模块 DLL 从 `<mod根>/MetabolicEssential/` 子目录改为直接输出到 `<mod根>/Assemblies/`。同时发现用户已把工程目录 `_metabolicEssentialsExtended` 改名为 `_metabolicEssentialsExtendedProj`。
- **这推翻了昨天定的"绝不进 Assemblies"铁律**，语义随之变化：RimWorld 会无条件载入 Assemblies 下所有 DLL，所以开关不再控制"是否载入"，只控制"是否调 Init（PatchAll）"。已在模块类注释里写死禁令：**模块内不得使用 [StaticConstructorOnStartup] 或有副作用的字段初始化器**，否则绕过开关。
- 换来的好处：XML Def 可以直接引用模块内类型（旧方案 Def 解析早于 Assembly.LoadFrom，做不到）。
- 改动：
  - `MetabolicLoader.cs` 重写：删掉 `Assembly.LoadFrom` 与 `ModuleDir/ModuleDll` 常量；改为遍历 `pack.assemblies.loadedAssemblies` 反射找 `IMetabolicModule` 实现。新增 `IsModulePresent` 属性（程序集在位，与开关无关），保留 `IsLoaded`（已 Init）。
  - `RimHormonesMod.cs` 设置 UI：先判 `!IsModulePresent` → 提示"未检测到 MetabolicEssential.dll"，否则 `Enable != IsLoaded` → 提示需重启。
  - `MetabolicEssential.csproj`：`OutputPath=..\Assemblies\` + `AppendTargetFrameworkToOutputPath=false` + `GenerateDependencyFile=false` + `ProduceReferenceAssembly=false`（产物只有 dll+pdb，无 deps.json）。
  - `copyToRimWorld.bat` 再次重写为 6 步：build 主工程 → **先把 RimHormones.dll 暂存进 Assemblies\**（模块工程编译期要引用，顺序不可调）→ build 模块（直落 Assemblies）→ 清理遗留 `MetabolicEssential/` 子目录（项目根+Steam）→ xcopy 内容目录 → `xcopy Assemblies\*.dll` 到 Steam + `dir /B` 打印实际部署清单。
- 用 rimsage 核实了 `Verse.ModAssemblyHandler.ReloadAll`：对 `Assemblies/` 下 `.dll` 逐个 `Assembly.LoadFrom`，来源 `GetAllFilesForModPreserveOrder`（`SearchOption.AllDirectories` 递归、按 `FileInfo.Name` 排序）→ 加载序 0Harmony → MetabolicEssential → RimHormones。模块先于其依赖载入，靠 Mono 的 basedir 同目录探测解析（RimWorld 多 DLL 互引通行做法）。
- 结果：两工程 0 错 0 警；`Assemblies/` = 0Harmony.dll + MetabolicEssential.dll(4608B) + RimHormones.dll(100864B)（+pdb）；旧 `MetabolicEssential/` 子目录已从项目根与 Steam 删除；Steam 侧 Assemblies 三 DLL 就位。DLL 内容已 grep 校验（`IsModulePresent` 存在、`ModuleDir` 归零）。

## 四个代谢需求 Need_MEE_*（已完成并部署）
- 用户指令：在 MetabolicEssential 模块内创建四个需求类 `Need_MEE_Water / Need_MEE_Sugar(用户误拼 suger，已纠正为 Sugar) / Need_MEE_Electrolytes / Need_MEE_Protein`。
- 实现：新增 `Source/MEEState.cs`（`public static bool Active` 模块启用标志）+ `Source/Need_MEE_Base.cs`（抽象基类 `Need_MEE_Base : Need`，含 `FallPerDay` 抽象属性、`Thresholds` 虚属性、`MaxLevel=1`、`SetInitialLevel=满值`、`NeedInterval` 按 `FallPerDay*150/60000` 衰减、`Satisfy(amount)` 外部补充 API、`Severity`）+ 四个子类各覆盖 `FallPerDay`（水0.6/糖0.4/电解质0.3/蛋白0.15 每日消耗比例，占位值待调）。
- **开关控制**：基类 `NeedInterval` 前置 `if (!MEEState.Active) return;`，`MetabolicEssentialModule.Init` 置 `MEEState.Active=true` → 开关关闭时需求保持满值零副作用；开启(Init)后开始消耗。契合"勾选需重启生效"架构。
- 四个 `NeedDef` XML 落到主 mod `Defs/NeedDefs/`（`needClass=MetabolicEssential.Need_MEE_X`，`baseLevel=1`、`fallPerDay=0`、`colonistsOnly=true`、`minIntelligence=ToolUser`），defName 为 `MEEWater/MEESugar/MEEElectrolytes/MEEProtein`。因模块 DLL 已在 Assemblies/，XML 能直接引用模块类型。
- 六个新 .cs 已加进 `MetabolicEssential.csproj` 的 `<Compile Include>` 列表（EnableDefaultCompileItems=false，漏加会静默不进 DLL）。
- 编译：模块工程 0 错 0 警，产物 MetabolicEssential.dll(5632B)；已部署到项目 `Assemblies/` 与 Steam `Mods/Rim-Hormones/Assemblies/`，四个 XML 已 xcopy 到 Steam。
- ⚠️ 已知行为：因 DLL 常驻 Assemblies/，四个需求**即使开关关闭也会出现在需求栏（恒满值不动）**；开关只控制是否消耗/生效，无法让需求栏彻底不显示（需条件加载 NeedDef，涉及 Def 加载期 patch，尚未做）。

## 架构二次反转：需求搬回主mod + MEE DLL 回退子目录（已完成+部署）
- 用户诉求澄清：**"在主体 Rim-Hormones 默认加载这些 need，但是不激活"** → 需求类留在主 mod 永远实例化/常驻需求栏；代谢 *逻辑* 拆进独立 DLL 做项目管理，不进 Assemblies/（回退到启用前版本）。
- **回退动作**：
  - `MetabolicLoader.cs` 重写回 `<modRoot>/MetabolicEssential/MetabolicEssential.dll` 的 `Assembly.LoadFrom` 版本；`IsModulePresent => File.Exists(ModulePath())`；成功 `Init` 后置 `MetabolicState.Active=true`（替代原 `MEEState`）。
  - 新增主 mod 静态类 `Hormones.MetabolicState`（`_indexProj/Source/Metabolic/MetabolicState.cs`）+ 加进 `Assembly-CSharp.csproj` 编译列表。
  - 四个 `Need_MEE_*` + `Need_MEE_Base` 从 `_metabolicEssentialsExtendedProj/Source/` **搬回** `_indexProj/Source/Needs/`，命名空间 `MetabolicEssential`→`Hormones`，`MEEState.Active`→`MetabolicState.Active`；4 个 `NeedDef` XML 的 `needClass` 改为 `Hormones.Need_MEE_X`。
  - MEE 工程：删掉 MEEState.cs + 4 个 Need + 基类（孤儿文件已 rm）；`MetabolicEssential.csproj` 仅留 `MetabolicEssentialModule.cs` 一个 Compile，新增属性 `OutputPath=..\MetabolicEssential\`+`AppendTargetFrameworkToOutputPath=false`+`GenerateDependencyFile=false`+`ProduceReferenceAssembly=false`（产物落到 mod 根 `MetabolicEssential\` 子目录，非 Assemblies）。
  - `MetabolicEssentialModule.cs` 去掉 `MEEState.Active=true`，注释说明"激活需求由主 mod 的 MetabolicLoader 统一置 MetabolicState.Active"。
  - `copyToRimWorld.bat` 改为：MEE 产出到 `MetabolicEssential\`；Step4 防呆删除 `Assemblies\` 下残留 `MetabolicEssential.dll`（防绕过开关）；Step6 额外把 `MetabolicEssential\` 拷到 Steam 同名子目录。
- 编译：两工程均 0 错 0 警。类型归属校验通过（Need_MEE_*/MetabolicState 在 RimHormones.dll；MetabolicEssential.dll 仅含 MetabolicEssentialModule）。
- 部署：Steam `Assemblies/` = 0Harmony.dll + RimHormones.dll（无 MetabolicEssential）；Steam `MetabolicEssential/` = MetabolicEssential.dll；4 个 MEE XML 已 xcopy 到 Steam。旧 `Assemblies/MetabolicEssential.dll` 残留已清理。
- 行为：需求栏**始终**显示四个代谢需求；模块关闭时恒满值、零消耗（"不激活"）；勾选开关+重启后 `MetabolicState.Active=true`，需求按 FallPerDay 开始消耗。Explainer: 现在"关闭=完全不载入 DLL"（最干净），代价是 XML 不能直接写 `Class="MetabolicEssential.XXX"`（但需求已在本体，无需）。
- **显示/隐藏控制（用户追加）**：新增 `MetabolicState.IsLoadedMME` 静态标志（与 `Active` 同步由 `MetabolicLoader.TryLoad` 在 Init 成功后置 true，TryLoad 开头先置 false）。`Need_MEE_Base.ShowOnNeedList => MetabolicState.IsLoadedMME` —— 模块未启用时四需求**隐藏**（不在需求栏），但依旧被主 mod 实例化、不消耗、零副作用；启用+重启后显示。编译两工程 0 错 0 警；已重新编译 MEE（引用更新后的 RimHormones.dll）并部署 Steam。优势：比"patch 私有 AddNeed / 条件加载 NeedDef"更稳，且后期开启模块时既有殖民者无需补挂需求即显示。
- **食谱显隐框架（MEE 食谱 gate，用户确认可行）**：RecipeDef 无 `ShowOnNeedList` 等价物，游戏改用 `RecipeDef.AvailableNow`（public getter，被 ITab_Bills 添加账单菜单/健康卡手术/物品可达性/任务生成引用）决定显隐与可否制作。新增两套主 mod 文件（`Source/Metabolic/`）：`MEERecipeMarker.cs`（`DefModExtension` 标记类）+ `RecipeDef_AvailableNow_MEE_Patch.cs`（Harmony postfix 拦截 `RecipeDef.AvailableNow` getter，当 `__result==true && !MetabolicState.IsLoadedMME && __instance.HasModExtension<MEERecipeMarker>()` 时翻成 false）。patch 必须放主 mod（常驻），因模块关掉时 MEE DLL 未载入仍需隐藏。csproj 已加 2 个 `<Compile Include>`。当前**尚无任何 MEE 食谱 XML**；用法：食谱 Def 的 `<modExtensions>` 加 `<li Class="Hormones.MEERecipeMarker" />` 即被 gate。编译主工程 0 错 0 警；部署 Steam（Assemblies 仅 0Harmony+RimHormones，MEE 在子目录）。

- **创建 4 个基础 MEE 物品（2026-08-09 晚）**：`Defs/ThingDefs/` 下 `Thing_MEE_RawSugar.xml`(生砂糖)、`Thing_MEE_Salt.xml`(盐)、`Thing_MEE_ProteinExtract.xml`(蛋白质提取物)、`Thing_MEE_WaterBottle.xml`(水瓶)。均 `ParentName="ResourceBase"`（category=Item、resource=true、ThingWithComps），对应四代谢需求（糖/电解质/蛋白/水）。贴图暂用 vanilla 占位（Silver/Steel/Gold/MedicineHerbal），注释标注后续可换专用贴图。翻译：`Languages/{ChineseSimplified,English}/DefInjected/ThingDef/MEE_Items.xml`（label+description，中文名依用户：生砂糖/盐/蛋白质提取物/水瓶）。6 个 XML 经 py xml 解析全 OK（修了一个 EN 文件 `.description` 误用 `.label` 闭合标签的笔误）。已 cp Defs+Languages 到 Steam。注意：物品**无** ShowOnNeedList 类隐藏机制，当前为常驻 def（关闭 MEE 时仍可在资源标签页看到），彻底隐藏需拆独立子 mod——已记入 MEMORY.md。消耗逻辑（Ingest 调 `Need_MEE_X.Satisfy`）尚未接，属后续工作。

- **生成 MEE 设计 Excel（2026-08-09 深夜）**：`文档/MEE设计.xlsx`（openpyxl，managed venv python 生成）。6 表：总览 / 代谢需求(4) / 基础物品(4) / 加载与开关 / 食谱门控框架 / 待补充(后续填)。原则"只描述已有的，后续作者补充"——已实现标绿、占位/待补标黄，『待补充』表留空白列供填。记录了已实现架构：4需求+4物品+开关框架(IsLoadedMME/Active)+食谱门控(MEERecipeMarker+AvailableNow patch)。⚠️ 顺带发现一个 UI 文案 bug：`RimHormonesMod.cs` 设置界面第25行提示「应位于本 mod 的 Assemblies 目录」已过时（DLL 现位于 `MetabolicEssential/` 子目录），需用户有空时改文案。
