# 2026-08-18

## MEE 三需求容量随体魄动态缩放
- 需求：水/电解质/蛋白质的 MaxLevel 从硬编码 1f 改为随体魄阶段变化，高体魄=更大容量=更强抗风险能力。
- 实现模式：参照 `Need_MuscleStrain.MaxLevel => PhysiqueLgc.GetMuscleStrainMax(pawn)` 的已有模式。
- 改动文件（4 个）：
  1. `PhysiqueDefine.cs`：新增 15 个常量（水/电/蛋白 × 5 阶段偏移量），数值取自用户表格。
  2. `PhysiqueLgc.cs`：新增 3 个静态方法 `GetMEEWaterCapacityMult`/`GetMEEElectrolytesCapacityMult`/`GetMEEProteinCapacityMult`，按 PhysiqueStage 返回 1.0+偏移 的倍率。
  3. `Need_MEE_Water.cs`：override `MaxLevel => GetMEEWaterCapacityMult(pawn)`（0.90~1.45）。
  4. `Need_MEE_Electrolytes.cs`：override `MaxLevel => GetMEEElectrolytesCapacityMult(pawn)`（0.90~1.30）。
  5. `Need_MEE_Protein.cs`：override `MaxLevel => GetMEEProteinCapacityMult(pawn)`（0.90~1.30）。
- 数值效果：卓越体魄小人水储量比虚弱高 61%（1.45 vs 0.90），断水存活时间从 ~1.64 天延长到 ~2.64 天；电解质/蛋白同理从 3.33 天延到 4.33 天。

## MEE 糖需求容量也随体魄缩放（补做）
- 用户后续追加：糖 factor = 0/0/0.1/0.15/0.2（虚弱/一般/健康/强壮/卓越）。与水/电/蛋白不同，糖的虚弱与一般为 0（**不惩罚弱者**，仅高体魄增益）。
- 改动文件（3 个）：`PhysiqueDefine.cs` 新增 5 常量 `MEESugarCap{Frail..Peak}`；`PhysiqueLgc.cs` 新增 `GetMEESugarCapacityMult`；`Need_MEE_Sugar.cs` 加 `override MaxLevel => GetMEESugarCapacityMult(pawn)`。
- 糖 MaxLevel：虚弱/一般=1.00、健康=1.10、强壮=1.15、卓越=1.20。
- 已 dotnet build 主工程+模块均 0 错 0 警，部署（md5 一致，方法名已编入 DLL）。
- 已 dotnet build 主工程+模块均 0 错 0 警，同步部署（md5 一致）。

## 更新 MEE设计.xlsx（rimworld-mod-sync 技能）
- 用户要求更新 Excel 反映 MEE 建筑/物品/配方/属性，并称配方可能有问题。
- 写了 `C:/Users/zhou/AppData/Local/Temp/mee_parse.py`（解析全部 *MEE*.xml 的 ThingDef/RecipeDef，做交叉校验）+ `build_excel.py`（生成表格）。
- 结果：保留原 8 个手写 sheet（总览/代谢需求/基础物品/加载与开关/食谱门控框架/负面效果/待补充/雨水收集系统），重写「基础物品」(9 物品)，新增「建筑」(2 收集器)、「配方」(17 配方)、「问题」(5 项) sheet；并在过时的「雨水收集系统」表尾加提示。
- 真实配方问题（已写入 问题 sheet）：
  1. 葡萄糖糖浆产出失衡：玉米 10→5(2:1) vs 水果 10→1(10:1)，玉米效率是水果 5 倍。
  2. 蛋白质提取物注释写"烹饪台"但 recipeUsers=ElectricSmelter（熔炼炉）。
  3. 葡萄糖基础配方 label 用 "glucose mash"，其余与物品本体用 "glucose syrup"。
  4. 雨水收集系统 sheet 仍引用旧名 MEE_RainwaterCollector。
- 同步到 Obsidian（xlsx2sheet.py）：先覆盖了「Excel/Excel 2026-08-16...sheet.md」（旧导入）；后按用户指定同步到「Rim-Hormones/同步/MEE设计.sheet.md」（verify OK 11 sheets/713 cells/36625B/LF-only，旧文件自动 .bak_20260818231242）。XML 修复未动。
  5. MEE_MilkTea 同时 Fluid+MealAwful+Nut0.33（既是饮又是餐）。
- 原文件已备份为 `MEE设计.xlsx.bak_<时间戳>`。未改动 XML（等用户确认是否修）。
- 注：物品 category 由 ParentName 继承（ResourceBase→Item），解析时以是否含 costList 判建筑。

## 同步 MEE设计.xlsx → Obsidian .sheet.md（rimworld-mod-sync route C）
- 用户："你先更新到md的sheet中" → 先把更新后的 Excel 同步到 Obsidian 表格插件格式。
- vault 表格落点：`F:\ObsdianProj\边缘世界mod开发本地\边缘世界Mod开发\Rim-Hormones\文档\MetaBolic\Excel\`
- 目标文件 `Excel 2026-08-16 08.17.41.sheet.md` 即 MEE设计 旧版导入（含原 7 sheet 无建筑/配方/问题）→ 直接覆盖（脚本自动备份 .bak_20260818225302）。
- 用 `xlsx2sheet.py` 转换（非插件原生导入，因用户要求我执行）：11 sheet / 713 cells / 36625 B / LF-only，verify OK。
- ⚠️ 脚本只做 JSON round-trip 校验，不验证 Obsidian 实际渲染；已提醒用户回 Obsidian 打开确认三张新表（建筑/配方/问题）正常显示。
- XML 配方修复暂未做（用户先要 md 同步）。

## MEE 适配 DBH 水系统 — 方案设计
- 用户要求出一套「MME 适配 DBH(DubsBadHygiene) 水系统」的 md 方案。已探查 DBH 1.6 源码/Def + 当前 MEE 水系统。
- DBH 水系统实测：NeedDef `DBHThirst`(needClass Need_Thirst)；喝水三途径=带 `WaterExt` 扩展物品(`water`/`SeekForThirst`)/`DBHDrinkFromGround` 地表/`DBHDrinkFromBasin` 设施；水网=WaterInlet→WaterPumpingStation→Pipe(mode=Sewage)→WaterStorage(WaterStorageCap)。识别可饮水物品的唯一依据是 `WaterExt` DefModExtension（纯 XML 加即可，无需引用 DBH 程序集）。
- MEE 现状：Need_MEE_Water(FallPerDay0.55, MaxLevel 随体魄0.90~1.45, Drink())；Comp_MEE_Satisfier(needDef=MEEWater)；水物品 MEE_WaterBottle/MEE_RawWater/各饮料；JobGiver_SatisfyMetabolic 自动补水+MEE_DrinkFromGround 地表；Comp_MEE_WaterCollector 产 MEE_RawWater。代码库无现成 DBH/mod 检测。
- 方案文档：`文档/MEE适配DBH水系统方案.md`。核心原则=DBH 在场时 DBH 为水真相源。三方案：A 适配器(MEE_Water 只读跟随 DBHThirst，体魄改影响后果)、B 仅兼容共存(MVP，加 WaterExt+关 MEE 自动补水/地表)、C 完全合并(移除 MEEWater)。推荐 B→A 两阶段。
- 关键实现：`DBHDetector`(StaticConstructorOnStartup 探测 `DefDatabase<NeedDef>.GetNamed("DBHThirst")!=null`)；水物品加 `WaterExt` modExtension；JobGiver 水分支/MEE_DrinkFromGround 在 DBH 模式返回 null；收集器可选反射灌 DBH WaterStorage(`DBHWaterBridge`)。零硬引用 DBH。
- 文档末尾列 4 条待确认决策（路线/收集器接水网/体魄缩放保留/水条显示）。未落地代码。

## MEE 适配 DBH — 方案 A 详细实现（WaterAdapter）
- 用户："按照方案A，做一个 WaterAdapter，含 GetWaterNeed，初始化时若 DBH 有就返回 DBH 的；并考虑相关属性修改（含 ground 补水 thing 用哪个）"。
- 新文档：`文档/MEE适配DBH_方案A_WaterAdapter.md`。核心设计：
  1. `WaterAdapter`（新增 `Source/Comps/WaterAdapter.cs`）：枚举 System{MEE,DBH}；`Init()` 依 `DBHDetector.DBHPresent` 设 Active；`GetWaterNeed(pawn)` 返回 `TryGetNeed(DBHThirst)` 或 `TryGetNeed<Need_MEE_Water>()`；辅助 `GetWaterLevel/IsThirsty/GetThirstUrgencyThreshold/GetWaterHazardTolerance`（后两者把体魄倍率 GetMEEWaterCapacityMult 用于阈值/后果层）。
  2. 初始化：`DBHDetector`（复用）+ `RimHormonesMod` 入口显式 `WaterAdapter.Init()` 双保险；`About/loadAfters` 加 DBH packageId `836308268`。
  3. DBH 模式**不实例化 Need_MEE_Water**（Harmony patch `Pawn_NeedsTracker.ShouldHaveNeed` 返回 false），消除双水条；UI 改用 WaterAdapter 显示 DBH 水位。
  4. **ground 补水 thing 分歧（重点）**：MEE 模式=MEE_RawWater 物品或水格(Drink())；DBH 模式 MEE 完全不主动喝地表水(JobGiver 水分支 return null)，地表/设施喝水全交 DBH(`DBHDrinkFromGround` 地形地表水/`DBHDrinkFromBasin` 水网设施)。DBH 地表水是地形级(IsSurfaceWater/TerrainWater/GetGroundWaterCapacity)，非物品级——此点与 MEE 物品级根本不同。
  5. 体魄缩放：MEE 模式作用于 MaxLevel(0.90~1.45)；DBH 模式转阈值/后果层(渴更晚/更抗水中毒)，不碰 DBH 存储。
  6. XML：6 个 MEE 水物品加 `WaterExt`(water/SeekForThirst)；收集器 DBH 模式走 B.1(加 WaterExt 让 DBH 认领) 而非 B.2(反射灌网)。
  7. 全代码库消费方改造清单（主工程 8 文件 + 模块 3 文件）已逐点列出。
- 待定：收集器 B.1 vs B.2、DBH 模式飘字/水中毒是否反射订阅 DBH 事件(建议先降级)、是否加 B.2 注入体魄倍率到 DBH 存储(默认不做)。未落地代码。

## MEE 适配 DBH — 逆置版落地：WaterAdapter 实现（MEE 独家水权威）
- 用户最终选择**逆置版**：不重定向 DBH 喝水，而是**关掉 DBHThirst、MEE 独家管水**。DBH 的渴 AI(ConditionalThirst→JobGiver_DrinkWater) 因需求缺失自动停摆，殖民者只喝 MEE_RawWater + MEE_DrinkFromGround(地形水，已是 defName.Contains("Water") 判定)。DBH 卫生管网(淋浴/马桶)照常。零 patch 进 BadHygiene.dll。
- 关键发现：MEE_DrinkFromGround **已经**是地形级（IsWaterCell 用 TerrainDef.defName 含 "Water" = 河/海/池塘），无需再改——用户以为要改，实际已完成。
- 落地文件（均已编译+同步部署，md5 一致）：
  - 新增 `_metabolicEssentialsExtendedProj/Source/WaterAdapter.cs`：MEE 加载(模块 Init)时触发 `Init()` → 检测 `DefDatabase<NeedDef>.GetNamedSilentFail("DBHThirst")` → 若 DBH 共存，用 `AccessTools.Method(typeof(Pawn_NeedsTracker),"ShouldHaveNeed")` 手动 `harmony.Patch` 一个 Postfix `DBHThirstSuppressor`，把 DBHThirst 的 ShouldHaveNeed 结果置 false。含 `GetWaterNeed(pawn)` 统一入口(返回 Need_MEE_Water)。零硬引用 DBH。
  - `MetabolicEssentialModule.cs`：`Init()` 末尾加 `WaterAdapter.Init();`（放在 MEEMgr.Register 之后）。
  - `MetabolicEssential.csproj`：加 `<Compile Include="Source\WaterAdapter.cs" />`（EnableDefaultCompileItems=false 必须显式注册）。
  - `About/About.xml`：加 `<loadAfters><li>Dubwise.DubsBadHygiene</li></loadAfters>`（**注意：DBH 真实 packageId 是 `Dubwise.DubsBadHygiene`，不是文件夹名 836308268**——初版填错已修正）。
- 坑：① `Pawn_NeedsTracker.ShouldHaveNeed` 是 **private**，必须 AccessTools 显式定位 + 手动 Patch，且**不能**加 `[HarmonyPatch]` 标注（否则被模块 Init 的 PatchAll 重复注册）。② `NeedDef`/`Need` 是 **RimWorld 命名空间**（非 Verse），文件需 `using RimWorld;`。③ `MetaBolicLoadCtrl.Active` 在 `inst.Init(pack)` **之后**才置 true，故 WaterAdapter.Init 内**不能**用 Active 当闸门（那时还是 false）；运行时 Postfix 内再校验 Active（此时已 true）。
- 验证：模块 `dotnet build -c Release` 0 错 0 警；DLL 已同步部署(md5 一致)，grep 确认 `DBHThirst` 字符串编入。game 未运行未遇锁。
- 待用户完全重启游戏测试：DBH+MEE 共存时应只显示 MEE 单水条、喝 MEE_RawWater/地形水，DBH 水瓶/水塔无人喝（无害）。仍建议盯日志看 DBH 在非 DBHThirst 处是否有 NRE（标准 ShouldHaveNeed 关闭法，成熟 mod 一般有 null 防护）。
