# 2026-07-25 工作记录

## 彻底清理废弃的皮质醇 Hediff 系统（09:48–09:56）
用户确认旧的 `Cortisol` Hediff 已废弃，要求清除残留。

### 删除/清理清单
1. **`Hediff_Cortisol.xml`**：移除废弃 `Cortisol` HediffDef（原 label「皮质醇（废弃）」，无 comps、纯占位）。
2. **`HormonesComponent.cs`**：删除 `AddCortisolHediff()` 方法、`cortisolAdded` 字段及其 CompTick 内调用（不再给 pawn 挂空 Hediff）。
3. **`HediffComp_Cortisol.cs`**：整文件删除（含 `HediffComp_Cortisol` 类、`HediffCompProperties_Cortisol`、`CortisolLogic` 静态类）。该文件所有内容均无外部引用。
4. **`Assembly-CSharp.csproj`**：移除 `<Compile Include="Source\Comps\HediffComp_Cortisol.cs" />`（否则 CS2001）。
5. **`Define.cs`**：删除 9 个旧版死常量（CortisolBaseDecay/BaseGrowthMin/Max/NeurastheniaGrowth/AdrenalineLinkGrowth/HungerGrowth/UglyEnvGrowth/PainGrowth/LowMoodGrowth）及「旧版常量」注释（全项目零引用）。
6. **`PhysiqueLgc.cs`**：头部注释的消费者列表划掉 `HediffComp_Cortisol`。

### 顺手修复的隐藏 bug
- 旧 `CortisolLogic.GetCortisolSeverity` 走的是废弃 Hediff（`GetCortisolComp`→固定 Severity 0.15），导致 Harmony 的「冒犯/侮辱互动加权」功能实际读死数据、永远返回 ×0.5（机制已悄悄失效）。
- 修复：`Need_Cortisol` 新增静态 `GetCortisolSeverity(Pawn)`（读 `CurLevel/MaxLevel`）；`Harmony_CortisolInteraction.cs` 改为 `Need_Cortisol.GetCortisolSeverity(initiator)`。现读真实浓度。

### 编译 & 部署
- `dotnet build -c Release` → **0 错误 0 警告**。
- 已同步 Defs 到 Steam 模组目录，DLL 成功覆盖（`Assemblies/RimHormones.dll` 09:56）。**本次游戏进程未锁定，部署直接成功**。
- 现行生效的皮质醇数据载体仅剩：`Need_Cortisol`（数值）+ `CortisolStatus` Hediff（健康面板显示，由 `Need_Cortisol.AddCortisolStatusHediff` 添加）。

## 肾上腺素模块重构调研与方案（设计讨论阶段，未动 C#）
- 用户选“保留 Hediff 只做修复”方向，但澄清设计视角：**肾上腺素效果最终都落在 Pawn 的“容量/忍耐度”体系**（视觉/听觉/呼吸/意识=PawnCapacity，减痛参考 Tough 特性的 painFactor）。纠正了“意识无对应项/减痛不值得接”的旧判断。
- 浓度分档现状：Adrenaline Hediff(Severity 0~1) 分 4 档 Dormant<0.15 / Low<0.5 / Medium<0.75 / High，阈值与用户表完全吻合。CalculateAdrenalineEffects 算 11 项，但仅 MoveSpeed(Harmony StatWorker_GetValue) + Metabolism(Harmony Need_Food) 注入，其余 9 项白算。
- 已改：Hediff_Adrenaline.xml 删除 4 个 stage 的 statFactors（重复 MoveSpeed），保留 stage 标签（待填 capacityMods）。
- 统一方案（待用户拍板）：
  - A 路 容量类 → Hediff stage 原生 capacityFactors / painFactor（Sight/Hearing/Breathing/Consciousness/BloodFiltration + painFactor 减痛），零代码随档切换，无重复加成。
  - B 路 Stat 类 → CalculateAdrenalineEffects + Harmony 注入（MoveSpeed / MeleeDamageFactor / MeleeHitChance / MeleeDodgeChance / RestRateMultiplier / Metabolism）。
- RimWorld 关键事实：Sight/Hearing/Breathing/Consciousness/BloodFiltration 是 **PawnCapacity（非 StatDef）**；Dodge 实际叫 MeleeDodgeChance；MeleeDamage 对应 MeleeDamageFactor；减痛无 Stat 对应，用 painFactor。
- Define.cs 的 AdrenalineLow/Medium/High 常量已备好各档数值（Consciousness/VisionReduction/HearingReduction/RespiratoryCirculatory/PainReduction/MeleeDamage/Dodge/MeleeHitReduction + AdrenalineRestMultiplier*），方案定后直接套。
- 与皮质醇耦合：Need_Cortisol.GetAdrenalineSeverity() 读 Adrenaline Hediff（联动阈值 0.5），保留 Hediff 体系则无需改。
- 状态：用户"先不着急开发"，等待方案/数值确认后再动 C# 与 XML。

## 肾上腺素模块重构落地（开发完成，10:15 部署）
- 采用统一方案：A路（容量/减痛/休息）走 HediffStage 原生字段 = 零代码随档切换；B路（近战/闪避/命中 Stat）走 Harmony 注入。
- XML：Defs/HediffDefs/Hediff_Adrenaline.xml 的 4 个 stage 填入：
  - capacityMods（PawnCapacity 加法 offset）：Sight/Hearing 取 Vision/HearingReduction（负，降低视听）；Breathing/Consciousness/BloodFiltration 取 RespiratoryCirculatory/BloodFiltration（正，供氧/专注/循环提升）。
  - painFactor = 1 + PainReduction（Low0.93/Med0.87/High0.80，耐痛）。
  - restFallFactor = 1 / RestMultiplier（Low1.0/Med0.8/High0.667，高浓度清醒时休息下降更慢=更抗疲劳/不困）。
  - Dormant(0档) 空效果。数值直接取自 Define.cs 的 AdrenalineLow/Medium/High（静态，不含体魄修正）。
- C#：HormonesLogic.cs 的 StatWorker_GetValue_Patch 在 effects.HasActiveEffects 内补全：MeleeDamageFactor ×(1+MeleeDamage)、MeleeDodgeChance ×(1+Dodge)、MeleeHitChance ×(1+MeleeHitReduction)（负值=应激手抖降命中）。MoveSpeed/Metabolism 此前已注入。
- 编译 dotnet build -c Release → 0 错误 0 警告。已 cp 部署 XML + DLL 到 Steam 模组（游戏未运行，直接覆盖成功）。重启游戏生效。
- 关键事实：A路容量修正为静态 XML，对所有 pawn 一视同仁（无体魄动态）；B路 Stat 注入保留 physiqueMod。与皮质醇联动 Need_Cortisol.GetAdrenalineSeverity() 仍读 Adrenaline Hediff，体系未改。

## 撤销 "restFallFactor 改为 Need_Food" 改动（10:26）
- 用户在"肾上腺素重构落地"后要求把 restFallFactor 改为经 Need_Food 表达（在 Need_Food_FallRate_Adrenaline_Patch 注入 effects.RestMultiplier，并移除 XML 的 restFallFactor）。
- 改动已做且编译通过，但用户复核后表示"已经有了 不用添加了" → 撤销该回合改动。
- 还原内容：Hediff_Adrenaline.xml 三个 stage 恢复 restFallFactor(1.0/0.8/0.667)；HormonesLogic.cs 的 Need_Food 补丁移除 effects.RestMultiplier 注入，仅保留 effects.Metabolism。
- 重新 dotnet build -c Release → 0 错误 0 警告。源码与 10:15 已部署状态一致，无需重新部署。
- 结论：肾上腺素"休息/能量"维度目前仍以 XML restFallFactor 表达（静态、抗疲劳/不困语义），未走 Need_Food。

## restFallFactor 注释掉（10:27）
- 用户要求将 Hediff_Adrenaline.xml 三个 stage 的 restFallFactor(1.0/0.8/0.667) 用 XML 注释禁用（保留不生效），替代上一回合撤销后的"保留生效"状态。
- 已实现：三处改为 `<!-- <restFallFactor>...</restFallFactor> -->`。当前肾上腺素"休息/能量"维度完全不生效（既无 XML restFallFactor 也无 Need_Food 注入）。
- 仅同步 XML 到 Steam 模组，无需重编译 DLL。重启游戏生效。
